<Window x:Class="ReadyCode.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReadyCode.Views"
        xmlns:models="clr-namespace:ReadyCode.Models"
        xmlns:coremodels="clr-namespace:ReadyCode.Models;assembly=ReadyCode.Core"
        xmlns:converters="clr-namespace:ReadyCode.Converters"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="{Binding ViewModel.Title}"
        Height="700"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown"
        AllowDrop="True"
        PreviewDragEnter="Window_PreviewDragOver"
        PreviewDragOver="Window_PreviewDragOver"
        PreviewDrop="Window_PreviewDrop">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:C64UMountedPathConverter x:Key="C64UMountedPathConverter"/>
        <converters:IntSumConverter x:Key="IntSumConverter"/>
        <converters:PendingCompareHighlightConverter x:Key="PendingCompareHighlightConverter"/>
        <converters:DebugVariableValueConverter x:Key="DebugVariableValueConverter"/>

        <!-- Activity bar icon toggle button (left bar — active indicator on left edge) -->
        <Style x:Key="ActivityBarButton"
                TargetType="ToggleButton">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Width"
                    Value="40"/>
            <Setter Property="Height"
                    Value="40"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Foreground"
                    Value="#9D9D9D"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="Transparent"
                                BorderThickness="2,0,0,0"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeActivityBarIndicator}"/>
                                <Setter Property="Foreground"
                                        Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter Property="Foreground"
                                        Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Activity bar icon toggle button (right bar — active indicator on right edge) -->
        <Style x:Key="ActivityBarButtonRight"
                TargetType="ToggleButton">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Width"
                    Value="40"/>
            <Setter Property="Height"
                    Value="40"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Foreground"
                    Value="#9D9D9D"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="Transparent"
                                BorderThickness="0,0,2,0"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeActivityBarIndicator}"/>
                                <Setter Property="Foreground"
                                        Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter Property="Foreground"
                                        Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Debug panel tab strip toggle button (horizontal — active indicator on bottom edge) -->
        <Style x:Key="DebugPanelTabToggle"
                TargetType="ToggleButton">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Padding"
                    Value="12,6"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="FontSize"
                    Value="11"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeTabInactiveFg}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeActivityBarIndicator}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource ThemeTabActiveFg}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter Property="Foreground"
                                        Value="{DynamicResource ThemeTabActiveFg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Errors panel severity filter chip (Errors/Warnings/Messages). Explicit template so
             its checked/hover/disabled visuals come entirely from the app's own theme resources -
             a plain ToggleButton with no Template relies on the OS default chrome for its checked
             state, which paints a fixed light-blue background regardless of theme and made the
             light dark-theme text on the checked "Errors" chip unreadable. -->
        <Style x:Key="ErrorsSeverityToggle"
                TargetType="ToggleButton">
            <Setter Property="Background"
                    Value="{DynamicResource ThemeSettingsBtnBg}"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeSettingsLabelFg}"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource ThemeSettingsBtnBorder}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Padding"
                    Value="8,3"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="FocusVisualStyle"
                    Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ThemeSettingsTreeSelectedBg}"/>
                                <Setter TargetName="border"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeSettingsAccent}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ThemeSettingsBtnHoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                    Value="False">
                                <Setter Property="Opacity"
                                        Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Status bar flat button chrome - borderless, transparent, hover highlight only (no
             checked state of its own). Shared base for the Caps Lock indicator below, which
             layers its own on/off Opacity DataTrigger on top via BasedOn. -->
        <Style x:Key="StatusBarFlatButton"
                TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeStatusBarFg}"/>
            <Setter Property="Opacity"
                    Value="0.5"/>
            <Setter Property="FocusVisualStyle"
                    Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="5,0,5,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ThemeMenuItemHoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                    Value="False">
                                <Setter Property="Opacity"
                                        Value="0.35"/>
                                <Setter Property="Cursor"
                                        Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Same flat chrome as StatusBarFlatButton, but always fully opaque - for a status bar
             segment that's just clickable (e.g. Lines), not an on/off indicator. -->
        <Style x:Key="StatusBarClickableButton"
                TargetType="Button"
                BasedOn="{StaticResource StatusBarFlatButton}">
            <Setter Property="Opacity"
                    Value="1"/>
        </Style>

        <!-- Column headers shared by every panel DataGrid (Errors, Debug Variables/Breakpoints).
             Without an explicit style, DataGridColumnHeader falls back to the OS default chrome,
             which paints a fixed light background/gray text regardless of app theme - unreadable
             in Dark/C64 theme (the same class of bug the severity chips above had). Uses
             ThemeFolderExplorerHeaderBg/ThemeFileFg rather than ThemePanelHeaderBg/Fg - the
             latter stays a near-black bar in every theme (by design, for other chrome), while
             this pair is the one actually used for every other panel header in the app (Folder
             Explorer, Variables count bar, PETSCII Reference, Quick Keys, the Errors toolbar row
             directly above this grid...) and genuinely tracks light/dark/C64 theme. -->
        <Style x:Key="PanelDataGridColumnHeaderStyle"
                TargetType="DataGridColumnHeader">
            <Setter Property="Background"
                    Value="{DynamicResource ThemeFolderExplorerHeaderBg}"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeFileFg}"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="Padding"
                    Value="8,6"/>
            <Setter Property="HorizontalContentAlignment"
                    Value="Left"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource ThemeDataGridLineBrush}"/>
            <Setter Property="BorderThickness"
                    Value="0,0,1,1"/>
        </Style>

        <!-- Context menu for editor tabs -->
        <ContextMenu x:Key="TabContextMenu"
                Opened="TabContextMenu_Opened">
            <MenuItem Header="Close"
                    InputGestureText="Ctrl+F4"
                    Click="TabContextClose_Click"/>
            <MenuItem Header="Close Others"
                    Click="TabContextCloseOthers_Click"/>
            <MenuItem Header="Close to the Right"
                    Click="TabContextCloseToRight_Click"/>
            <MenuItem Header="Close Saved"
                    InputGestureText="Ctrl+K U"
                    Click="TabContextCloseSaved_Click"/>
            <MenuItem Header="Close All"
                    InputGestureText="Ctrl+K W"
                    Click="TabContextCloseAll_Click"/>
            <Separator/>
            <MenuItem Header="Copy Path"
                    Click="TabContextCopyPath_Click"/>
        </ContextMenu>

        <!-- Context menu for the text editor -->
        <ContextMenu x:Key="EditorContextMenu">
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="EditorContextTransfer_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="EditorContextViceTransfer_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="EditorContextRun_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="EditorContextViceRun_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Go to Definition"
                    InputGestureText="F12"
                    Click="EditorContextGoToLineNumber_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="EditCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="EditCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="EditPaste_Click"/>
            <Separator/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="EditDelete_Click"/>
            <Separator/>
            <MenuItem Header="Minify Code"
                    InputGestureText="Ctrl+M"
                    Click="EditorContextMinify_Click"/>
            <MenuItem Header="Prettify Code"
                    InputGestureText="Ctrl+Shift+M"
                    Click="EditorContextPrettify_Click"/>
            <MenuItem Header="Renumber Code"
                    InputGestureText="Ctrl+R"
                    Click="EditorContextRenumber_Click"/>
        </ContextMenu>

        <!-- Context menu for the Hex Editor - shares the same Cut/Copy/Paste/Delete handlers as
             EditorContextMenu above, which already branch on ActiveTab.IsHexMode. -->
        <ContextMenu x:Key="HexEditorContextMenu">
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="EditCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="EditCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="EditPaste_Click"/>
            <Separator/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="EditDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for the root Explorer header -->
        <ContextMenu x:Key="RootFolderContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="RootContextPaste_Click"/>
        </ContextMenu>

        <!-- Context menu for folder items in the Explorer tree -->
        <ContextMenu x:Key="FolderContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="New File..."
                    Click="FolderContextNewFile_Click"/>
            <MenuItem Header="New Folder..."
                    Click="FolderContextNewFolder_Click"/>
            <MenuItem Header="New .d64 Disk Image..."
                    Click="FolderContextNewD64_Click"/>
            <MenuItem Header="New .d81 Disk Image..."
                    Click="FolderContextNewD81_Click"/>
            <Separator/>
            <MenuItem Header="Reveal in File Explorer"
                    Click="FolderContextReveal_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="FolderContextCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="FolderContextCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="FolderContextPaste_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="FolderContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="FolderContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for file items in the Explorer tree -->
        <ContextMenu x:Key="FileContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="Open in Hex editor"
                    Click="FileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Reveal in File Explorer"
                    Click="FileContextReveal_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="FileContextCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="FileContextCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="FileContextPaste_Click"/>
            <Separator/>
            <MenuItem Header="Copy Path"
                    Click="FileContextCopyPath_Click"/>
            <MenuItem Header="Copy Relative Path"
                    Click="FileContextCopyRelativePath_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="FileContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="FileContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for .bas files in the Explorer tree - same as FileContextMenu but with an
             "Open in BASIC editor" item, matching double-click. Untokenized ASCII source, not a
             ready-to-transfer program, so unlike .prg it has no Load/Run items. -->
        <ContextMenu x:Key="FileBasicContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="FileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="FileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Reveal in File Explorer"
                    Click="FileContextReveal_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="FileContextCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="FileContextCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="FileContextPaste_Click"/>
            <Separator/>
            <MenuItem Header="Copy Path"
                    Click="FileContextCopyPath_Click"/>
            <MenuItem Header="Copy Relative Path"
                    Click="FileContextCopyRelativePath_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="FileContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="FileContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for .prg files confirmed to be BASIC in the Explorer tree - same as
             FileBasicContextMenu but with Load/Run submenus, since a tokenized .prg is already a
             ready-to-transfer program. -->
        <ContextMenu x:Key="FilePrgContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="FileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="FileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="FileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="FileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="FileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="FileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Reveal in File Explorer"
                    Click="FileContextReveal_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="FileContextCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="FileContextCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="FileContextPaste_Click"/>
            <Separator/>
            <MenuItem Header="Copy Path"
                    Click="FileContextCopyPath_Click"/>
            <MenuItem Header="Copy Relative Path"
                    Click="FileContextCopyRelativePath_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="FileContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="FileContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for machine-language .prg files in the Explorer tree - same as
             FileContextMenu but with a "Disassemble file" item and Load/Run submenus. -->
        <ContextMenu x:Key="FileMlContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="Disassemble file"
                    Click="FileContextDisassemble_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="FileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="FileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="FileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="FileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="FileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Reveal in File Explorer"
                    Click="FileContextReveal_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="FileContextCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="FileContextCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="FileContextPaste_Click"/>
            <Separator/>
            <MenuItem Header="Copy Path"
                    Click="FileContextCopyPath_Click"/>
            <MenuItem Header="Copy Relative Path"
                    Click="FileContextCopyRelativePath_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="FileContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="FileContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for .asm/.s assembly source files in the Explorer tree - same as
             FileContextMenu but with an "Open in Assembly editor" item and Load/Run submenus
             (assembled fresh from source before transferring). -->
        <ContextMenu x:Key="FileAsmContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="Open in Assembly editor"
                    Click="FileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="FileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="FileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="FileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="FileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="FileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Reveal in File Explorer"
                    Click="FileContextReveal_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="FileContextCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="FileContextCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="FileContextPaste_Click"/>
            <Separator/>
            <MenuItem Header="Copy Path"
                    Click="FileContextCopyPath_Click"/>
            <MenuItem Header="Copy Relative Path"
                    Click="FileContextCopyRelativePath_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="FileContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="FileContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" BASIC entries found inside a mounted .d64/.d81 image in the
             Folder Explorer tree - other kinds use their own dedicated menu below (Prg, Ml, Asm, or
             hex-only for anything unrecognized). Untokenized ASCII source, so no Load/Run items. -->
        <ContextMenu x:Key="LocalDiskEntryContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="LocalDiskEntryContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="LocalDiskEntryContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="LocalDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="LocalDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" Prg entries found inside a mounted .d64/.d81 image in the
             Folder Explorer tree. -->
        <ContextMenu x:Key="LocalDiskEntryPrgContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="LocalDiskEntryContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="LocalDiskEntryContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="LocalDiskEntryContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="LocalDiskEntryContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="LocalDiskEntryContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="LocalDiskEntryContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="LocalDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="LocalDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" machine-language entries found inside a mounted .d64/.d81
             image in the Folder Explorer tree. -->
        <ContextMenu x:Key="LocalDiskEntryMlContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Disassemble file"
                    Click="LocalDiskEntryContextDisassemble_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="LocalDiskEntryContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="LocalDiskEntryContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="LocalDiskEntryContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="LocalDiskEntryContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="LocalDiskEntryContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="LocalDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="LocalDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" assembly-source entries found inside a mounted .d64/.d81
             image in the Folder Explorer tree. -->
        <ContextMenu x:Key="LocalDiskEntryAsmContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in Assembly editor"
                    Click="LocalDiskEntryContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="LocalDiskEntryContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="LocalDiskEntryContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="LocalDiskEntryContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="LocalDiskEntryContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="LocalDiskEntryContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="LocalDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="LocalDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" entries of an unrecognized kind found inside a mounted
             .d64/.d81 image in the Folder Explorer tree - no text-editor view makes sense for
             these, so only the hex view is offered. -->
        <ContextMenu x:Key="LocalDiskEntryOtherContextMenu">
            <MenuItem Header="Open in Hex editor"
                    Click="LocalDiskEntryContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="LocalDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="LocalDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for a .d64/.d81 disk image node itself in the Folder Explorer tree -->
        <ContextMenu x:Key="LocalDiskImageContextMenu"
                Opened="PasteContextMenu_Opened">
            <MenuItem Header="Open in Hex editor"
                    Click="FileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Add File..."
                    Click="LocalDiskImageContextAddFile_Click"/>
            <Separator/>
            <MenuItem Header="Reveal in File Explorer"
                    Click="FileContextReveal_Click"/>
            <Separator/>
            <MenuItem Header="Cut"
                    InputGestureText="Ctrl+X"
                    Click="FileContextCut_Click"/>
            <MenuItem Header="Copy"
                    InputGestureText="Ctrl+C"
                    Click="FileContextCopy_Click"/>
            <MenuItem Header="Paste"
                    InputGestureText="Ctrl+V"
                    Click="FileContextPaste_Click"/>
            <Separator/>
            <MenuItem Header="Copy Path"
                    Click="FileContextCopyPath_Click"/>
            <MenuItem Header="Copy Relative Path"
                    Click="FileContextCopyRelativePath_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="FileContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="FileContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for folder items in the C64U tree -->
        <ContextMenu x:Key="C64UFolderContextMenu">
            <MenuItem Header="New Folder..."
                    Click="C64UFolderContextNewFolder_Click"/>
            <MenuItem Header="New .d64 Disk Image..."
                    Click="C64UFolderContextNewD64_Click"/>
            <MenuItem Header="New .d81 Disk Image..."
                    Click="C64UFolderContextNewD81_Click"/>
            <MenuItem Header="Refresh"
                    Click="C64UFolderContextRefresh_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="C64UContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="C64UContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for .bas items in the C64U tree. Untokenized ASCII source, not a
             ready-to-transfer program, so unlike .prg it has no Load/Run items. -->
        <ContextMenu x:Key="C64UBasicFileContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="C64UFileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Download to PC..."
                    Click="C64UFileContextDownload_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="C64UContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="C64UContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for .prg items confirmed to be BASIC in the C64U tree. -->
        <ContextMenu x:Key="C64UPrgFileContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="C64UFileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="C64UFileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="C64UFileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="C64UFileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="C64UFileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Download to PC..."
                    Click="C64UFileContextDownload_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="C64UContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="C64UContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for genuinely unrecognized (Other) items in the C64U tree - non-runnable,
             non-disk-image, and not Prg/Ml/Asm (which get their own dedicated menus below). No
             text-editor view makes sense for an unrecognized kind, so only the hex view is offered. -->
        <ContextMenu x:Key="C64UOtherFileContextMenu">
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Download to PC..."
                    Click="C64UFileContextDownload_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="C64UContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="C64UContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for machine-language (Ml) items in the C64U tree. -->
        <ContextMenu x:Key="C64UMlFileContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Disassemble file"
                    Click="C64UFileContextDisassemble_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="C64UFileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="C64UFileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="C64UFileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="C64UFileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Download to PC..."
                    Click="C64UFileContextDownload_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="C64UContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="C64UContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for assembly-source (Asm) items in the C64U tree. -->
        <ContextMenu x:Key="C64UAsmFileContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in Assembly editor"
                    Click="C64UFileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="C64UFileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="C64UFileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="C64UFileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="C64UFileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Download to PC..."
                    Click="C64UFileContextDownload_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="C64UContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="C64UContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for .d64/.d81 disk image items in the C64U tree -->
        <ContextMenu x:Key="C64UDiskImageContextMenu">
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Add File..."
                    Click="C64UDiskImageContextAddFile_Click"/>
            <Separator/>
            <MenuItem Header="Mount to Drive A"
                    Click="C64UFileContextMountA_Click"/>
            <MenuItem Header="Mount to Drive B"
                    Click="C64UFileContextMountB_Click"/>
            <Separator/>
            <MenuItem Header="Download to PC..."
                    Click="C64UFileContextDownload_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    InputGestureText="F2"
                    Click="C64UContextRename_Click"/>
            <MenuItem Header="Delete"
                    InputGestureText="Delete"
                    Click="C64UContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" BASIC entries found inside a mounted .d64/.d81 image - other
             kinds use their own dedicated menu below (Prg, Ml, Asm, or hex-only for anything
             unrecognized). Untokenized ASCII source, so no Load/Run items. -->
        <ContextMenu x:Key="C64UDiskEntryContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="C64UFileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="C64UDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="C64UDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" Prg entries found inside a mounted .d64/.d81 image in the
             C64U tree. -->
        <ContextMenu x:Key="C64UDiskEntryPrgContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in BASIC editor"
                    Click="C64UFileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="C64UFileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="C64UFileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="C64UFileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="C64UFileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="C64UDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="C64UDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" machine-language entries found inside a mounted .d64/.d81
             image in the C64U tree. -->
        <ContextMenu x:Key="C64UDiskEntryMlContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Disassemble file"
                    Click="C64UFileContextDisassemble_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="C64UFileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="C64UFileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="C64UFileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="C64UFileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="C64UDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="C64UDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" assembly-source entries found inside a mounted .d64/.d81
             image in the C64U tree. -->
        <ContextMenu x:Key="C64UDiskEntryAsmContextMenu"
                Opened="CompareContextMenu_Opened">
            <MenuItem Header="Open in Assembly editor"
                    Click="C64UFileContextOpen_Click"/>
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Select file for comparison"
                    Click="CompareSelectFile_Click"/>
            <MenuItem Header="Compare file"
                    Click="CompareWithSelectedFile_Click"/>
            <Separator/>
            <MenuItem Header="Load">
                <MenuItem Header="C64U"
                        InputGestureText="Ctrl+F5"
                        Click="C64UFileContextLoadC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Ctrl+Alt+F5"
                        Click="C64UFileContextLoadVice_Click"/>
            </MenuItem>
            <MenuItem Header="Run">
                <MenuItem Header="C64U"
                        InputGestureText="F5"
                        Click="C64UFileContextRunC64U_Click"/>
                <MenuItem Header="VICE"
                        InputGestureText="Alt+F5"
                        Click="C64UFileContextRunVice_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="C64UDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="C64UDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Context menu for "virtual" entries of an unrecognized kind found inside a mounted
             .d64/.d81 image in the C64U tree - no text-editor view makes sense for these, so only
             the hex view is offered. -->
        <ContextMenu x:Key="C64UDiskEntryOtherContextMenu">
            <MenuItem Header="Open in Hex editor"
                    Click="C64UFileContextOpenAsHex_Click"/>
            <Separator/>
            <MenuItem Header="Rename..."
                    Click="C64UDiskEntryContextRename_Click"/>
            <MenuItem Header="Delete"
                    Click="C64UDiskEntryContextDelete_Click"/>
        </ContextMenu>

        <!-- Shared visual template for Explorer/C64U tree items: expand arrow, hover/selection/drop
             highlighting. Each tree's own ItemContainerStyle derives from this and adds its own
             context menu selection (and, for the Explorer, drag-reorder event wiring). -->
        <Style x:Key="ExplorerTreeItemStyle"
                TargetType="TreeViewItem">
            <Setter Property="IsExpanded"
                    Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- Full-row background spans both columns -->
                                <Border x:Name="RowBg"
                                        Grid.ColumnSpan="2"
                                        Background="Transparent"
                                        SnapsToDevicePixels="True"/>
                                <!-- Expand/collapse toggle -->
                                <ToggleButton x:Name="Expander"
                                              Grid.Column="0"
                                              Focusable="False"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              ClickMode="Press">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Background"
                                                    Value="Transparent"/>
                                            <Setter Property="BorderThickness"
                                                    Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border Background="Transparent"
                                                                Width="16"
                                                                Height="16">
                                                            <Path x:Name="Arrow"
                                                                  Data="M 5 3 L 11 8 L 5 13 Z"
                                                                  Fill="#888888"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked"
                                                                    Value="True">
                                                                <Setter TargetName="Arrow"
                                                                        Property="Data"
                                                                        Value="M 3 5 L 13 5 L 8 11 Z"/>
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver"
                                                                    Value="True">
                                                                <Setter TargetName="Arrow"
                                                                        Property="Fill"
                                                                        Value="#555555"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                <!-- Item content (file/folder name). Stretches to fill the row so
                                     right-aligned content (e.g. the C64U tree's type badge) can
                                     hug the pane's right edge rather than sitting immediately
                                     after the name. -->
                                <ContentPresenter x:Name="PART_Header"
                                                  Grid.Column="1"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Center"
                                                  Margin="2,2"/>
                            </Grid>
                            <!-- Children shown when expanded -->
                            <ItemsPresenter x:Name="ItemsHost"
                                            Visibility="Collapsed"
                                            Margin="16,0,0,0"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded"
                                    Value="True">
                                <Setter TargetName="ItemsHost"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                            <Trigger Property="HasItems"
                                    Value="False">
                                <Setter TargetName="Expander"
                                        Property="Visibility"
                                        Value="Hidden"/>
                            </Trigger>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="RowBg"
                                        Property="Background"
                                        Value="{DynamicResource ThemeFolderExplorerHoverBg}"/>
                            </Trigger>
                            <!-- Active selection (overrides hover) -->
                            <Trigger Property="IsSelected"
                                    Value="True">
                                <Setter TargetName="RowBg"
                                        Property="Background"
                                        Value="{DynamicResource ThemeFileSelectedBg}"/>
                            </Trigger>
                            <!-- Inactive selection -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected"
                                            Value="True"/>
                                    <Condition Property="IsSelectionActive"
                                            Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="RowBg"
                                        Property="Background"
                                        Value="{DynamicResource ThemeFileInactiveSelectedBg}"/>
                            </MultiTrigger>
                            <!-- Drop target (overrides all) -->
                            <DataTrigger Binding="{Binding IsDropTarget}"
                                    Value="True">
                                <Setter TargetName="RowBg"
                                        Property="Background"
                                        Value="#FFF3CD"/>
                                <Setter TargetName="RowBg"
                                        Property="BorderBrush"
                                        Value="#FFC107"/>
                                <Setter TargetName="RowBg"
                                        Property="BorderThickness"
                                        Value="1"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small icon button for Explorer panel header -->
        <Style x:Key="ExplorerHeaderButton"
                TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Width"
                    Value="24"/>
            <Setter Property="Height"
                    Value="24"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeFolderExplorerIconFg}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="Transparent"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ThemeFolderExplorerBtnHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ThemeFolderExplorerBtnPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close (×) button inside each editor tab -->
        <Style x:Key="TabCloseButtonStyle"
                TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Width"
                    Value="16"/>
            <Setter Property="Height"
                    Value="16"/>
            <Setter Property="Padding"
                    Value="0"/>
            <Setter Property="Cursor"
                    Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="Transparent"
                                CornerRadius="2">
                            <TextBlock Text="&#xE711;"
                                    FontFamily="Segoe MDL2 Assets"
                                    FontSize="9"
                                       Foreground="#888888"
                                       HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="{DynamicResource ThemeFolderExplorerBtnHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button for inserting a PETSCII special character from the side panel -->
        <Style x:Key="SpecialCharButtonStyle"
                TargetType="Button">
            <Setter Property="Width"
                    Value="62"/>
            <Setter Property="Height"
                    Value="60"/>
            <Setter Property="Margin"
                    Value="3"/>
            <Setter Property="Padding"
                    Value="0"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Background"
                    Value="{DynamicResource ThemeCardBg}"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource ThemeFolderExplorerHeaderBorder}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeSpecialCharIconFg}"/>
            <Setter Property="FocusVisualStyle"
                    Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="{DynamicResource ThemeSpecialCharBtnHoverBg}"/>
                                <Setter TargetName="Bd"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeSpecialCharBtnHoverBorder}"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                    Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="{DynamicResource ThemeSpecialCharBtnPressedBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem container for editor tabs -->
        <Style x:Key="TabItemContainerStyle"
                TargetType="ListBoxItem">
            <Setter Property="Background"
                    Value="{DynamicResource ThemeTabInactiveBg}"/>
            <Setter Property="Padding"
                    Value="0"/>
            <Setter Property="Margin"
                    Value="0"/>
            <Setter Property="FocusVisualStyle"
                    Value="{x:Null}"/>
            <Setter Property="ContextMenu"
                    Value="{StaticResource TabContextMenu}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <!-- Outer border: right-side separator between tabs -->
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource ThemeTabItemBorder}"
                                BorderThickness="0,0,1,0">
                            <!-- Inner border: top accent (blue when active) -->
                            <Border x:Name="TopAccent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0,2,0,0">
                                <ContentPresenter/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected"
                                    Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource ThemeTabActiveBg}"/>
                                <Setter TargetName="TopAccent"
                                        Property="BorderBrush"
                                        Value="#0066CC"/>
                            </Trigger>
                            <!-- For PRG/ASM/BAS tabs, the active top accent matches that kind's Explorer
                                 badge color instead of the default blue - these triggers must come
                                 after the base IsSelected trigger above to win when both apply. -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}"
                                            Value="True"/>
                                    <Condition Binding="{Binding Kind}"
                                            Value="Prg"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="TopAccent"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeBadgePrgBg}"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}"
                                            Value="True"/>
                                    <Condition Binding="{Binding Kind}"
                                            Value="Asm"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="TopAccent"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeBadgeAsmBg}"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}"
                                            Value="True"/>
                                    <Condition Binding="{Binding Kind}"
                                            Value="Bas"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="TopAccent"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeBadgeBasBg}"/>
                            </MultiDataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ContextMenu popup container — full ControlTemplate removes WPF's hardcoded
             #F1F1F1 icon-column Rectangle that bleeds through as a white band. -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background"
                    Value="{DynamicResource ThemeMenuItemBg}"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeMenuItemFg}"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource ThemeMenuItemBorder}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Padding"
                    Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Shared glyph style for standard menu icons (New/Save/Print/Cut/Copy/Paste/etc.) -->
        <Style x:Key="MenuItemIconText"
                TargetType="TextBlock">
            <Setter Property="FontFamily"
                    Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize"
                    Value="13"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
        </Style>

        <!-- Global MenuItem style: themes top-level bar items and all dropdown/submenu items -->
        <Style TargetType="MenuItem">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource ThemeMenuItemFg}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <!-- check mark -->
                                    <ColumnDefinition Width="*"/>
                                    <!-- header text -->
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- gesture text -->
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- submenu arrow -->
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="CheckMark"
                                           Grid.Column="0"
                                           Text="✓"
                                           FontSize="11"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"
                                           Foreground="{TemplateBinding Foreground}"/>
                                <ContentPresenter Grid.Column="0"
                                                  ContentSource="Icon"
                                                  Width="16"
                                        Height="16"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                <AccessText x:Name="AccessText"
                                            Grid.Column="1"
                                            Text="{TemplateBinding Header}"
                                            Margin="0,4,8,4"
                                            VerticalAlignment="Center"/>
                                <TextBlock x:Name="GestureText"
                                           Grid.Column="2"
                                           Text="{TemplateBinding InputGestureText}"
                                           Foreground="{DynamicResource ThemeMenuItemFg}"
                                           Margin="16,4,8,4"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                                <TextBlock x:Name="SubArrow"
                                           Grid.Column="3"
                                           Text="›"
                                           FontSize="14"
                                           Margin="4,0,6,0"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Visibility="Collapsed"/>
                                <Popup x:Name="PART_Popup"
                                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       Placement="Bottom">
                                    <Border Background="{DynamicResource ThemeMenuItemBg}"
                                            BorderBrush="{DynamicResource ThemeMenuItemBorder}"
                                            BorderThickness="1"
                                            Padding="2">
                                        <ItemsPresenter/>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Top-level bar items: span AccessText over the check column so spacing is unchanged -->
                            <Trigger Property="Role"
                                    Value="TopLevelHeader">
                                <Setter TargetName="AccessText"
                                        Property="Grid.Column"
                                        Value="0"/>
                                <Setter TargetName="AccessText"
                                        Property="Grid.ColumnSpan"
                                        Value="2"/>
                                <Setter TargetName="AccessText"
                                        Property="Margin"
                                        Value="8,4,8,4"/>
                            </Trigger>
                            <Trigger Property="Role"
                                    Value="TopLevelItem">
                                <Setter TargetName="AccessText"
                                        Property="Grid.Column"
                                        Value="0"/>
                                <Setter TargetName="AccessText"
                                        Property="Grid.ColumnSpan"
                                        Value="2"/>
                                <Setter TargetName="AccessText"
                                        Property="Margin"
                                        Value="8,4,8,4"/>
                            </Trigger>
                            <!-- Submenu item: set popup bg as item bg, show gesture text -->
                            <Trigger Property="Role"
                                    Value="SubmenuItem">
                                <Setter TargetName="Root"
                                        Property="Background"
                                        Value="{DynamicResource ThemeMenuItemBg}"/>
                                <Setter TargetName="GestureText"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                            <!-- Submenu header: same bg, popup opens to the right, show arrow -->
                            <Trigger Property="Role"
                                    Value="SubmenuHeader">
                                <Setter TargetName="Root"
                                        Property="Background"
                                        Value="{DynamicResource ThemeMenuItemBg}"/>
                                <Setter TargetName="PART_Popup"
                                        Property="Placement"
                                        Value="Right"/>
                                <Setter TargetName="SubArrow"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                            <!-- Checked state: show the check mark glyph -->
                            <Trigger Property="IsChecked"
                                    Value="True">
                                <Setter TargetName="CheckMark"
                                        Property="Visibility"
                                        Value="Visible"/>
                            </Trigger>
                            <!-- Hover/highlight state (overrides role bg) -->
                            <Trigger Property="IsHighlighted"
                                    Value="True">
                                <Setter TargetName="Root"
                                        Property="Background"
                                        Value="{DynamicResource ThemeMenuItemHoverBg}"/>
                                <Setter TargetName="Root"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ThemeMenuItemHoverBorder}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource ThemeMenuItemHoverFg}"/>
                                <Setter TargetName="GestureText"
                                        Property="Foreground"
                                        Value="{DynamicResource ThemeMenuItemHoverFg}"/>
                                <Setter TargetName="SubArrow"
                                        Property="Foreground"
                                        Value="{DynamicResource ThemeMenuItemHoverFg}"/>
                            </Trigger>
                            <!-- Disabled state -->
                            <Trigger Property="IsEnabled"
                                    Value="False">
                                <Setter Property="Foreground"
                                        Value="{DynamicResource ThemeMenuItemDisabledFg}"/>
                                <Setter TargetName="GestureText"
                                        Property="Foreground"
                                        Value="{DynamicResource ThemeMenuItemDisabledFg}"/>
                            </Trigger>
                            <!-- No gesture text when InputGestureText is empty (overrides SubmenuItem trigger) -->
                            <DataTrigger Binding="{Binding InputGestureText, RelativeSource={RelativeSource TemplatedParent}}"
                                    Value="">
                                <Setter TargetName="GestureText"
                                        Property="Visibility"
                                        Value="Collapsed"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <!-- Input Bindings for Keyboard Shortcuts -->
    <Window.InputBindings>
        <!-- File Menu -->
        <KeyBinding Key="N"
                Modifiers="Ctrl"
                Command="{Binding FileNewCommand}"/>
        <KeyBinding Key="N"
                Modifiers="Ctrl+Shift"
                Command="{Binding FileNewBasCommand}"/>
        <KeyBinding Key="N"
                Modifiers="Ctrl+Alt"
                Command="{Binding FileNewAsmCommand}"/>
        <KeyBinding Key="O"
                Modifiers="Ctrl"
                Command="{Binding FileOpenCommand}"/>
        <KeyBinding Key="S"
                Modifiers="Ctrl"
                Command="{Binding FileSaveCommand}"/>
        <KeyBinding Key="S"
                Modifiers="Ctrl+Shift"
                Command="{Binding FileSaveAsCommand}"/>
        <KeyBinding Key="P"
                Modifiers="Ctrl"
                Command="{Binding ViewModel.FilePrintCommand}"/>

        <!-- Edit Menu -->
        <KeyBinding Key="G"
                Modifiers="Ctrl"
                Command="{Binding EditGoToLineCommand}"/>
        <KeyBinding Key="F12"
                Command="{Binding GoToDefinitionCommand}"/>
        <KeyBinding Key="Z"
                Modifiers="Ctrl"
                Command="{Binding EditUndoCommand}"/>
        <KeyBinding Key="Y"
                Modifiers="Ctrl"
                Command="{Binding EditRedoCommand}"/>
        <KeyBinding Key="X"
                Modifiers="Ctrl"
                Command="{Binding EditCutCommand}"/>
        <KeyBinding Key="C"
                Modifiers="Ctrl"
                Command="{Binding EditCopyCommand}"/>
        <KeyBinding Key="V"
                Modifiers="Ctrl"
                Command="{Binding EditPasteCommand}"/>
        <KeyBinding Key="Delete"
                Command="{Binding EditDeleteCommand}"/>
        <KeyBinding Key="U"
                Modifiers="Ctrl+Shift"
                Command="{Binding EditMakeUppercaseCommand}"/>
        <KeyBinding Key="L"
                Modifiers="Ctrl+Shift"
                Command="{Binding EditToggleLowerCaseModeCommand}"/>
        <KeyBinding Key="M"
                Modifiers="Ctrl"
                Command="{Binding EditMinifyCommand}"/>
        <KeyBinding Key="M"
                Modifiers="Ctrl+Shift"
                Command="{Binding EditPrettifyCommand}"/>
        <KeyBinding Key="R"
                Modifiers="Ctrl"
                Command="{Binding EditRenumberCommand}"/>
        <KeyBinding Key="F"
                Modifiers="Ctrl"
                Command="{Binding EditFindCommand}"/>
        <KeyBinding Key="H"
                Modifiers="Ctrl"
                Command="{Binding EditReplaceCommand}"/>
        <KeyBinding Key="F"
                Modifiers="Ctrl+Shift"
                Command="{Binding EditFindInFilesCommand}"/>
        <KeyBinding Key="H"
                Modifiers="Ctrl+Shift"
                Command="{Binding EditReplaceInFilesCommand}"/>

        <!-- Tab management -->
        <KeyBinding Key="F4"
                Modifiers="Ctrl"
                Command="{Binding TabCloseCurrentCommand}"/>
        <KeyBinding Key="T"
                Modifiers="Ctrl+Shift"
                Command="{Binding TabReopenClosedCommand}"/>

        <!-- View Menu -->
        <KeyBinding Key="B"
                Modifiers="Ctrl"
                Command="{Binding ViewPrimarySideBarCommand}"/>
        <KeyBinding Key="B"
                Modifiers="Ctrl+Alt"
                Command="{Binding ViewSecondarySideBarCommand}"/>
        <KeyBinding Key="E"
                Modifiers="Ctrl+Shift"
                Command="{Binding ViewExplorerCommand}"/>
        <KeyBinding Key="V"
                Modifiers="Ctrl+Alt"
                Command="{Binding ViewVariablesCommand}"/>
        <KeyBinding Key="Z"
                Modifiers="Alt"
                Command="{Binding ViewWordWrapCommand}"/>

        <!-- C64U Menu - Load has no shortcut of its own (it used to be Ctrl+F5, freed up for Run
             Without Debugging below to eliminate the conflict). -->
        <KeyBinding Key="F5"
                Modifiers="Ctrl"
                Command="{Binding ViewModel.C64URunCommand}"/>

        <!-- VICE Menu - see the C64U note above; Load's old Ctrl+Alt+F5 is freed the same way. -->
        <KeyBinding Key="F5"
                Modifiers="Ctrl+Alt"
                Command="{Binding ViewModel.ViceRunCommand}"/>

        <!-- Screen Control (Ctrl+1–8) -->
        <KeyBinding Key="D1"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="19"/>
        <KeyBinding Key="D2"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="147"/>
        <KeyBinding Key="D3"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="18"/>
        <KeyBinding Key="D4"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="146"/>
        <KeyBinding Key="D5"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="157"/>
        <KeyBinding Key="D6"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="29"/>
        <KeyBinding Key="D7"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="145"/>
        <KeyBinding Key="D8"
                Modifiers="Ctrl"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="17"/>

        <!-- Printing Colors CTRL+SHIFT+1–8: Black White Red Cyan Purple Green Blue Yellow -->
        <KeyBinding Key="D1"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="144"/>
        <KeyBinding Key="D2"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="5"/>
        <KeyBinding Key="D3"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="28"/>
        <KeyBinding Key="D4"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="159"/>
        <KeyBinding Key="D5"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="156"/>
        <KeyBinding Key="D6"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="30"/>
        <KeyBinding Key="D7"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="31"/>
        <KeyBinding Key="D8"
                Modifiers="Ctrl+Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="158"/>

        <!-- Printing Colors CTRL+SHIFT+ALT+1–8: Orange Brown Lt.Red Gray1 Gray2 Lt.Green Lt.Blue Gray3 -->
        <KeyBinding Key="D1"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="129"/>
        <KeyBinding Key="D2"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="149"/>
        <KeyBinding Key="D3"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="150"/>
        <KeyBinding Key="D4"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="151"/>
        <KeyBinding Key="D5"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="152"/>
        <KeyBinding Key="D6"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="153"/>
        <KeyBinding Key="D7"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="154"/>
        <KeyBinding Key="D8"
                Modifiers="Ctrl+Shift+Alt"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="155"/>

        <!-- Function Keys SHIFT+F1-F8. Shift+F3 only reaches this binding when the Find bar is
             closed - Window_PreviewKeyDown claims it for Find Previous while the bar is open.
             Shift+F5 is deliberately missing from this set (was character code 135) - it's now
             C64U's Stop Debugging below, which won this conflict; that PETSCII character is still
             reachable from the Special Characters panel. -->
        <KeyBinding Key="F1"
                Modifiers="Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="133"/>
        <KeyBinding Key="F2"
                Modifiers="Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="137"/>
        <KeyBinding Key="F3"
                Modifiers="Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="134"/>
        <KeyBinding Key="F4"
                Modifiers="Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="138"/>
        <KeyBinding Key="F6"
                Modifiers="Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="139"/>
        <KeyBinding Key="F7"
                Modifiers="Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="136"/>
        <KeyBinding Key="F8"
                Modifiers="Shift"
                Command="{Binding InsertSpecialCharCommand}"
                CommandParameter="140"/>

        <!-- Debug (C64U) - unmodified F-keys, matching the C64U menu's own shortcut column. -->
        <KeyBinding Key="F5"
                Command="{Binding ViewModel.DebugStartOrContinueOnC64UCommand}"/>
        <KeyBinding Key="F5"
                Modifiers="Ctrl+Shift"
                Command="{Binding ViewModel.DebugRestartOnC64UCommand}"/>
        <KeyBinding Key="F5"
                Modifiers="Shift"
                Command="{Binding ViewModel.DebugStopCommand}"/>
        <KeyBinding Key="F10"
                Command="{Binding ViewModel.DebugStepOverCommand}"/>
        <KeyBinding Key="F11"
                Command="{Binding ViewModel.DebugStepIntoCommand}"/>
        <KeyBinding Key="F11"
                Modifiers="Shift"
                Command="{Binding ViewModel.DebugStepOutCommand}"/>
        <KeyBinding Key="F10"
                Modifiers="Ctrl"
                Command="{Binding DebugRunToCursorCommand}"/>
        <KeyBinding Key="F9"
                Command="{Binding DebugToggleBreakpointCommand}"/>
        <KeyBinding Key="F9"
                Modifiers="Ctrl"
                Command="{Binding DebugToggleBreakpointEnabledCommand}"/>
        <KeyBinding Key="F9"
                Modifiers="Ctrl+Shift"
                Command="{Binding DebugDeleteAllBreakpointsCommand}"/>

        <!-- Debug (VICE) - same actions as the C64U set above, Alt-prefixed throughout, matching
             the VICE menu's own shortcut column and the established "Alt = VICE" convention
             already used by Run Without Debugging above. -->
        <KeyBinding Key="F5"
                Modifiers="Alt"
                Command="{Binding ViewModel.DebugStartOrContinueOnViceCommand}"/>
        <KeyBinding Key="F5"
                Modifiers="Ctrl+Alt+Shift"
                Command="{Binding ViewModel.DebugRestartOnViceCommand}"/>
        <KeyBinding Key="F5"
                Modifiers="Alt+Shift"
                Command="{Binding ViewModel.DebugStopCommand}"/>
        <KeyBinding Key="F10"
                Modifiers="Alt"
                Command="{Binding ViewModel.DebugStepOverCommand}"/>
        <KeyBinding Key="F11"
                Modifiers="Alt"
                Command="{Binding ViewModel.DebugStepIntoCommand}"/>
        <KeyBinding Key="F11"
                Modifiers="Alt+Shift"
                Command="{Binding ViewModel.DebugStepOutCommand}"/>
        <KeyBinding Key="F10"
                Modifiers="Ctrl+Alt"
                Command="{Binding DebugRunToCursorCommand}"/>
        <KeyBinding Key="F9"
                Modifiers="Alt"
                Command="{Binding DebugToggleBreakpointCommand}"/>
        <KeyBinding Key="F9"
                Modifiers="Ctrl+Alt"
                Command="{Binding DebugToggleBreakpointEnabledCommand}"/>
        <KeyBinding Key="F9"
                Modifiers="Ctrl+Alt+Shift"
                Command="{Binding DebugDeleteAllBreakpointsCommand}"/>

        <!-- Debug - Pause, same shared command both ways, matching the F5/F9/F10/F11 pattern above. -->
        <KeyBinding Key="F6"
                Command="{Binding ViewModel.DebugPauseCommand}"/>
        <KeyBinding Key="F6"
                Modifiers="Alt"
                Command="{Binding ViewModel.DebugPauseCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top"
                Background="{DynamicResource ThemeMenuBg}"
                Foreground="{DynamicResource ThemeMenuFg}"
                Height="28">

            <MenuItem Header="_File">
                <MenuItem Header="_New">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE7C3;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                    <MenuItem Header="_Program File (.prg)"
                            Command="{Binding FileNewCommand}"
                            InputGestureText="Ctrl+N"/>
                    <MenuItem Header="_BASIC File (.bas)"
                            Command="{Binding FileNewBasCommand}"
                            InputGestureText="Ctrl+Shift+N"/>
                    <MenuItem Header="_Assembly File (.asm)"
                            Command="{Binding FileNewAsmCommand}"
                            InputGestureText="Ctrl+Alt+N"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Open File..."
                        Command="{Binding FileOpenCommand}"
                        InputGestureText="Ctrl+O">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE8E5;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Open _Folder..."
                        Command="{Binding FileOpenFolderCommand}"
                        InputGestureText="Ctrl+K Ctrl+O"/>
                <MenuItem Header="Open _Recent"
                        x:Name="RecentFilesMenuItem">
                    <MenuItem Header="(none)"
                            IsEnabled="False"/>
                </MenuItem>
                <MenuItem Header="Reopen Closed _Tab"
                        Command="{Binding TabReopenClosedCommand}"
                        InputGestureText="Ctrl+Shift+T"/>
                <Separator/>
                <MenuItem Header="_Close Editor"
                        Command="{Binding TabCloseCurrentCommand}"
                        InputGestureText="Ctrl+F4"/>
                <MenuItem Header="Close Fo_lder"
                        Command="{Binding FileCloseFolderCommand}"
                        InputGestureText="Ctrl+K F"/>
                <Separator/>
                <MenuItem Header="_Save"
                        Command="{Binding FileSaveCommand}"
                        InputGestureText="Ctrl+S">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE74E;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Save _As..."
                        Command="{Binding FileSaveAsCommand}"
                        InputGestureText="Ctrl+Shift+S">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE792;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Export..."
                        Command="{Binding FileExportCommand}"/>
                <MenuItem Header="_Import..."
                        Command="{Binding FileImportCommand}"/>
                <Separator/>
                <MenuItem Header="Pa_ge Setup..."
                        Command="{Binding ViewModel.FilePageSetupCommand}"/>
                <MenuItem Header="Print Pre_view..."
                        Command="{Binding ViewModel.FilePrintPreviewCommand}"/>
                <MenuItem Header="_Print..."
                        Command="{Binding ViewModel.FilePrintCommand}"
                        InputGestureText="Ctrl+P">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE749;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="E_xit"
                        Click="FileExit_Click"/>
            </MenuItem>

            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo"
                        Command="{Binding EditUndoCommand}"
                        InputGestureText="Ctrl+Z">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE7A7;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Redo"
                        Command="{Binding EditRedoCommand}"
                        InputGestureText="Ctrl+Y">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE7A6;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Go to Line..."
                        Command="{Binding EditGoToLineCommand}"
                        InputGestureText="Ctrl+G"/>
                <Separator/>
                <MenuItem Header="Cu_t"
                        Command="{Binding EditCutCommand}"
                        InputGestureText="Ctrl+X">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE8C6;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Copy"
                        Command="{Binding EditCopyCommand}"
                        InputGestureText="Ctrl+C">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE8C8;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Paste"
                        Command="{Binding EditPasteCommand}"
                        InputGestureText="Ctrl+V">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE77F;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Delete"
                        Command="{Binding EditDeleteCommand}"
                        InputGestureText="Delete">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE74D;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="Comment _Selection"
                        Command="{Binding EditCommentCommand}"
                        InputGestureText="Ctrl+K Ctrl+C"/>
                <MenuItem Header="U_ncomment Selection"
                        Command="{Binding EditUncommentCommand}"
                        InputGestureText="Ctrl+K Ctrl+U"/>
                <Separator/>
                <MenuItem Header="M_ake Uppercase"
                        Command="{Binding EditMakeUppercaseCommand}"
                        InputGestureText="Ctrl+Shift+U"/>
                <MenuItem Header="Make L_owercase"
                        Command="{Binding EditMakeLowercaseCommand}"/>
                <Separator/>
                <MenuItem Header="_Lower Case Mode"
                        IsCheckable="True"
                        IsChecked="{Binding ViewModel.IsLowerCaseModeActive, Mode=TwoWay}"
                        IsEnabled="{Binding ViewModel.IsShiftModeApplicable}"
                        InputGestureText="Ctrl+Shift+L" />
                <Separator/>
                <MenuItem x:Name="MinifyMenuItem"
                        Header="Minif_y Code"
                        Command="{Binding EditMinifyCommand}"
                        InputGestureText="Ctrl+M"/>
                <MenuItem x:Name="PrettifyMenuItem"
                        Header="Prett_ify Code"
                        Command="{Binding EditPrettifyCommand}"
                        InputGestureText="Ctrl+Shift+M"/>
                <MenuItem x:Name="RenumberMenuItem"
                        Header="Renu_mber Code"
                        Command="{Binding EditRenumberCommand}"
                        InputGestureText="Ctrl+R"/>
                <MenuItem x:Name="FormatMenuItem"
                        Header="_Format Code"
                        Command="{Binding EditFormatCommand}"
                        InputGestureText="Ctrl+K Ctrl+F"
                        Visibility="Collapsed"/>
                <Separator/>
                <MenuItem Header="_Find"
                        Command="{Binding EditFindCommand}"
                        InputGestureText="Ctrl+F">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE721;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="R_eplace"
                        Command="{Binding EditReplaceCommand}"
                        InputGestureText="Ctrl+H"/>
                <Separator/>
                <MenuItem Header="Find in _Files"
                        Command="{Binding EditFindInFilesCommand}"
                        InputGestureText="Ctrl+Shift+F"/>
                <MenuItem Header="R_eplace in Files"
                        Command="{Binding EditReplaceInFilesCommand}"
                        InputGestureText="Ctrl+Shift+H"/>
            </MenuItem>

            <MenuItem Header="_View">
                <MenuItem Header="_Column Guide"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.ShowColumnGuide, Mode=TwoWay}"/>
                <Separator/>
                <MenuItem Header="_Primary Side Bar"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.IsLeftPanelOpen}"
                          Command="{Binding ViewPrimarySideBarCommand}"
                          InputGestureText="Ctrl+B"/>
                <MenuItem Header="_Secondary Side Bar"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.IsRightPanelOpen}"
                          Command="{Binding ViewSecondarySideBarCommand}"
                          InputGestureText="Ctrl+Alt+B"/>
                <Separator/>
                <MenuItem Header="_Explorer"
                          Command="{Binding ViewExplorerCommand}"
                          InputGestureText="Ctrl+Shift+E"/>
                <MenuItem Header="_Variables"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.ShowVariableExplorer, Mode=TwoWay}"
                          InputGestureText="Ctrl+Alt+V"/>
                <MenuItem Header="_Debug Panel"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.IsDebugPanelActive, Mode=TwoWay}"/>
                <MenuItem Header="E_rrors Panel"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.IsErrorsPanelActive, Mode=TwoWay}"/>
                <Separator/>
                <MenuItem Header="_Word Wrap"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.WordWrap, Mode=TwoWay}"
                          InputGestureText="Alt+Z"/>
                <Separator/>
                <MenuItem Header="Code S_tatistics"
                          Command="{Binding ViewCodeStatisticsCommand}"/>
            </MenuItem>

            <MenuItem Header="C64_U"
                    Visibility="{Binding ViewModel.ShowC64UMenu, Converter={StaticResource BoolToVisibility}}">
                <MenuItem Header="_Load"
                        Command="{Binding ViewModel.C64UTransferCommand}"/>
                <MenuItem Header="_Run Without Debugging"
                        Command="{Binding ViewModel.C64URunCommand}"
                        InputGestureText="Ctrl+F5">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE768;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Start Debugging / Continue"
                        Command="{Binding ViewModel.DebugStartOrContinueOnC64UCommand}"
                        InputGestureText="F5"/>
                <MenuItem Header="_Pause"
                        Command="{Binding ViewModel.DebugPauseCommand}"
                        InputGestureText="F6"/>
                <MenuItem Header="Restart De_bugging"
                        Command="{Binding ViewModel.DebugRestartOnC64UCommand}"
                        InputGestureText="Ctrl+Shift+F5"/>
                <MenuItem Header="Sto_p Debugging"
                        Command="{Binding ViewModel.DebugStopCommand}"
                        InputGestureText="Shift+F5"/>
                <Separator/>
                <MenuItem Header="Step _Over"
                        Command="{Binding ViewModel.DebugStepOverCommand}"
                        InputGestureText="F10"/>
                <MenuItem Header="Step _Into"
                        Command="{Binding ViewModel.DebugStepIntoCommand}"
                        InputGestureText="F11"/>
                <MenuItem Header="Step O_ut"
                        Command="{Binding ViewModel.DebugStepOutCommand}"
                        InputGestureText="Shift+F11"/>
                <MenuItem Header="R_un to Cursor"
                        Command="{Binding DebugRunToCursorCommand}"
                        InputGestureText="Ctrl+F10"/>
                <Separator/>
                <MenuItem Header="_Toggle Breakpoint"
                        Command="{Binding DebugToggleBreakpointCommand}"
                        InputGestureText="F9"/>
                <MenuItem Header="_Enable/Disable Breakpoint"
                        Command="{Binding DebugToggleBreakpointEnabledCommand}"
                        InputGestureText="Ctrl+F9"/>
                <MenuItem Header="_Delete All Breakpoints"
                        Command="{Binding DebugDeleteAllBreakpointsCommand}"
                        InputGestureText="Ctrl+Shift+F9"/>
                <Separator/>
                <MenuItem Header="R_eset"
                        Command="{Binding ViewModel.C64UResetCommand}"/>
                <MenuItem Header="Re_boot"
                        Command="{Binding ViewModel.C64URebootCommand}"/>
                <MenuItem Header="Power _Off"
                        Command="{Binding ViewModel.C64UPowerOffCommand}"/>
                <Separator/>
                <MenuItem Header="_Disassemble at..."
                        Click="C64UDisassemble_Click"/>
                <Separator/>
                <MenuItem Header="_About My C64U"
                        Command="{Binding ViewModel.C64USystemInfoCommand}"/>
            </MenuItem>

            <MenuItem Header="VI_CE"
                    Visibility="{Binding ViewModel.ShowViceMenu, Converter={StaticResource BoolToVisibility}}">
                <MenuItem Header="_Load"
                        Command="{Binding ViewModel.ViceTransferCommand}"/>
                <MenuItem Header="_Run Without Debugging"
                        Command="{Binding ViewModel.ViceRunCommand}"
                        InputGestureText="Ctrl+Alt+F5">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xE768;"
                                Style="{StaticResource MenuItemIconText}"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Start Debugging / Continue"
                        Command="{Binding ViewModel.DebugStartOrContinueOnViceCommand}"
                        InputGestureText="Alt+F5"/>
                <MenuItem Header="_Pause"
                        Command="{Binding ViewModel.DebugPauseCommand}"
                        InputGestureText="Alt+F6"/>
                <MenuItem Header="Restart De_bugging"
                        Command="{Binding ViewModel.DebugRestartOnViceCommand}"
                        InputGestureText="Ctrl+Alt+Shift+F5"/>
                <MenuItem Header="Sto_p Debugging"
                        Command="{Binding ViewModel.DebugStopCommand}"
                        InputGestureText="Alt+Shift+F5"/>
                <Separator/>
                <MenuItem Header="Step _Over"
                        Command="{Binding ViewModel.DebugStepOverCommand}"
                        InputGestureText="Alt+F10"/>
                <MenuItem Header="Step _Into"
                        Command="{Binding ViewModel.DebugStepIntoCommand}"
                        InputGestureText="Alt+F11"/>
                <MenuItem Header="Step O_ut"
                        Command="{Binding ViewModel.DebugStepOutCommand}"
                        InputGestureText="Alt+Shift+F11"/>
                <MenuItem Header="R_un to Cursor"
                        Command="{Binding DebugRunToCursorCommand}"
                        InputGestureText="Ctrl+Alt+F10"/>
                <Separator/>
                <MenuItem Header="_Toggle Breakpoint"
                        Command="{Binding DebugToggleBreakpointCommand}"
                        InputGestureText="Alt+F9"/>
                <MenuItem Header="_Enable/Disable Breakpoint"
                        Command="{Binding DebugToggleBreakpointEnabledCommand}"
                        InputGestureText="Ctrl+Alt+F9"/>
                <MenuItem Header="_Delete All Breakpoints"
                        Command="{Binding DebugDeleteAllBreakpointsCommand}"
                        InputGestureText="Ctrl+Alt+Shift+F9"/>
                <Separator/>
                <MenuItem Header="R_eset"
                        Command="{Binding ViewModel.ViceResetCommand}"/>
                <MenuItem Header="Re_boot"
                        Command="{Binding ViewModel.ViceRebootCommand}"/>
                <MenuItem Header="Power _Off"
                        Command="{Binding ViewModel.VicePowerOffCommand}"/>
                <Separator/>
                <MenuItem Header="_Disassemble at..."
                        Click="ViceDisassemble_Click"/>
                <Separator/>
                <MenuItem Header="_About VICE"
                        Command="{Binding ViewModel.ViceSystemInfoCommand}"/>
            </MenuItem>

            <MenuItem Header="_Preferences">
                <MenuItem Header="Show Status _Bar"
                          IsCheckable="True"
                          IsChecked="{Binding ViewModel.ShowStatusBar, Mode=TwoWay}"/>
                <Separator/>
                <MenuItem Header="_Settings..."
                        Command="{Binding PreferencesSettingsCommand}"/>
            </MenuItem>

            <MenuItem Header="_Help">
                <MenuItem Header="Visit READYCode on _GitHub"
                        Command="{Binding ViewModel.HelpGitHubCommand}"/>
                <MenuItem Header="View Online _Docs"
                        Command="{Binding ViewModel.HelpDocsCommand}"/>
                <Separator/>
                <MenuItem Header="_About READYCode"
                        Command="{Binding ViewModel.HelpAboutCommand}"/>
            </MenuItem>

        </Menu>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom"
                Background="{DynamicResource ThemeStatusBarBg}"
                Foreground="{DynamicResource ThemeStatusBarFg}"
                Height="24"
                   Visibility="{Binding ViewModel.ShowStatusBar, Converter={StaticResource BoolToVisibility}}">
            <StatusBarItem Margin="5,0,0,0">
                <Button Style="{StaticResource StatusBarClickableButton}"
                        Command="{Binding ViewCodeStatisticsCommand}"
                        ToolTip="Show Code Statistics">
                    <TextBlock Text="{Binding ViewModel.LineCountText}"
                            FontSize="11"
                            Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                </Button>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalContentAlignment="Center"
                    MinWidth="100"
                    Margin="5,0,0,0">
                <TextBlock Text="{Binding ViewModel.ScreenPositionText}"
                        FontSize="11"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right"
                    DockPanel.Dock="Right">
                <Button x:Name="CapsLockIndicator"
                        Click="CapsLockIndicator_Click"
                        ToolTip="Caps Lock - click to toggle">
                    <Button.Style>
                        <Style TargetType="Button"
                                BasedOn="{StaticResource StatusBarFlatButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ViewModel.IsCapsLockOn}"
                                        Value="True">
                                    <Setter Property="Opacity"
                                            Value="1"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72E;"
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="11"
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="CAPS"
                                FontSize="11"
                                VerticalAlignment="Center"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                    </StackPanel>
                </Button>
            </StatusBarItem>
            <!-- Shift Badge - toggles the active tab's C64 keyboard-emulation mode between "Upper
                 Active" (default: unshifted letters show upper case, Shift/Caps Lock shows the
                 PETSCII graphic for that key) and "Upper Inactive" (unshifted shows lower case,
                 Shift/Caps Lock shows upper case) - see PetsciiGlyphGenerator.IsUpperCaseModeActive.
                 Filled = Upper Active, outlined = Upper Inactive; dimmed (via the shared IsEnabled
                 trigger) whenever the active tab isn't an ordinary editable text tab. -->
            <StatusBarItem HorizontalAlignment="Right"
                    DockPanel.Dock="Right">
                <Button x:Name="ShiftModeIndicator"
                        IsEnabled="{Binding ViewModel.IsShiftModeApplicable}"
                        Click="ShiftModeIndicator_Click" 
                        ToolTip="Lower Case Mode - click to toggle">
                    <Button.Style>
                        <Style TargetType="Button"
                                BasedOn="{StaticResource StatusBarFlatButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ViewModel.IsShiftModeApplicable}"
                                        Value="True">
                                    <Setter Property="Opacity"
                                            Value="1"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Path Width="11"
                            Height="13"
                            Stretch="Uniform"
                            Data="M12,3 L19,11 L15,11 L15,20 L9,20 L9,11 L5,11 Z">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Fill"
                                        Value="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ViewModel.IsUpperCaseModeActive}"
                                            Value="False">
                                        <Setter Property="Fill"
                                                Value="Transparent"/>
                                        <Setter Property="Stroke"
                                                Value="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        <Setter Property="StrokeThickness"
                                                Value="1.4"/>
                                        <Setter Property="StrokeLineJoin"
                                                Value="Round"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left">
                <StatusBarItem.Style>
                    <Style TargetType="StatusBarItem">
                        <Setter Property="Background"
                                Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ViewModel.StatusType}"
                                    Value="Warning">
                                <Setter Property="Background"
                                        Value="{DynamicResource ThemeStatusWarningBg}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ViewModel.StatusType}"
                                    Value="Error">
                                <Setter Property="Background"
                                        Value="{DynamicResource ThemeStatusErrorBg}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StatusBarItem.Style>
                <TextBlock Text="{Binding ViewModel.StatusMessage}"
                        FontSize="11">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground"
                                    Value="{DynamicResource ThemeStatusBarFg}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ViewModel.StatusType}"
                                        Value="Warning">
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource ThemeStatusWarningFg}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ViewModel.StatusType}"
                                        Value="Error">
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource ThemeStatusErrorFg}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>

        <!-- Main content: activity bars + side panels + editor -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <!-- Left activity bar -->
                <ColumnDefinition x:Name="LeftPanelCol"
                        Width="0"
                        MinWidth="0"/>
                <!-- Left panel -->
                <ColumnDefinition x:Name="LeftSplitterCol"
                        Width="0"
                        MinWidth="0"/>
                <!-- Left splitter -->
                <ColumnDefinition Width="*"
                        MinWidth="100"/>
                <!-- Editor -->
                <ColumnDefinition x:Name="RightSplitterCol"
                        Width="0"
                        MinWidth="0"/>
                <!-- Right splitter -->
                <ColumnDefinition x:Name="RightPanelCol"
                        Width="0"
                        MinWidth="0"/>
                <!-- Right panel -->
                <ColumnDefinition Width="40"/>
                <!-- Right activity bar -->
            </Grid.ColumnDefinitions>

            <!-- Left activity bar -->
            <Border Grid.Column="0"
                    Background="#3C3C3C"
                    BorderBrush="#2D2D2D"
                    BorderThickness="0,0,1,0">
                <StackPanel VerticalAlignment="Top">
                    <ToggleButton x:Name="ExplorerToggle"
                                  Style="{StaticResource ActivityBarButton}"
                                  ToolTip="Explorer"
                                  Click="ExplorerToggle_Click">
                        <TextBlock Text="&#xE8B7;"
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="18"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                    <ToggleButton x:Name="C64UToggle"
                                  Style="{StaticResource ActivityBarButton}"
                                  ToolTip="C64U"
                                  Click="C64UToggle_Click">
                        <TextBlock Text="&#xE7F4;"
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="18"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                    <ToggleButton x:Name="SearchToggle"
                                  Style="{StaticResource ActivityBarButton}"
                                  ToolTip="Search (Ctrl+Shift+F)"
                                  Click="SearchToggle_Click">
                        <TextBlock Text="&#xE721;"
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="18"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                </StackPanel>
            </Border>

            <!-- Left panel column: Explorer/C64U (mutually exclusive, toggled) on top, Variable
                 Explorer always visible below - independent of which of those two is active. -->
            <Grid x:Name="ExplorerColumnGrid"
                    Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="FolderTreeRow"
                            Height="250"
                            MinHeight="60"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition x:Name="VariablesRow"
                            Height="*"
                            MinHeight="60"/>
                </Grid.RowDefinitions>

                <!-- Left panel: Explorer (file tree) -->
                <Grid x:Name="ExplorerPanel"
                        Grid.Row="0">
                    <DockPanel Background="{DynamicResource ThemeFolderExplorerBg}">
                        <Border x:Name="ExplorerHeaderBorder"
                                DockPanel.Dock="Top"
                                Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                                BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                                BorderThickness="0,0,0,1"
                                Height="32"
                                AllowDrop="True"
                                ContextMenu="{StaticResource RootFolderContextMenu}"
                                DragOver="RootHeader_DragOver"
                                DragLeave="RootHeader_DragLeave"
                                Drop="RootHeader_Drop">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding ViewModel.ExplorerTitle}"
                                           FontSize="11"
                                        FontWeight="Bold"
                                        Foreground="{DynamicResource ThemeFileFg}"
                                           VerticalAlignment="Center"
                                        Margin="10,0,0,0"/>
                                <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                        Margin="0,0,4,0">
                                    <Button Style="{StaticResource ExplorerHeaderButton}"
                                            ToolTip="New File"
                                            Click="ExplorerNewFile_Click">
                                        <TextBlock Text="&#xE7C3;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="13"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    </Button>
                                    <Button Style="{StaticResource ExplorerHeaderButton}"
                                            ToolTip="New Folder"
                                            Click="ExplorerNewFolder_Click">
                                        <TextBlock Text="&#xED25;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="13"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    </Button>
                                    <Button Style="{StaticResource ExplorerHeaderButton}"
                                            ToolTip="Refresh Explorer"
                                            Click="ExplorerRefresh_Click">
                                        <TextBlock Text="&#xE72C;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="13"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    </Button>
                                    <Button Style="{StaticResource ExplorerHeaderButton}"
                                            ToolTip="Collapse Folders in Explorer"
                                            Click="ExplorerCollapse_Click">
                                        <TextBlock Text="&#xE8D8;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="13"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <TreeView x:Name="FileTree"
                                  Background="{DynamicResource ThemeFolderExplorerBg}"
                                  BorderThickness="0"
                                  AllowDrop="True"
                                  ItemsSource="{Binding ViewModel.FolderItems}"
                                  MouseDoubleClick="FileTree_MouseDoubleClick"
                                  PreviewMouseLeftButtonDown="FileTree_PreviewMouseLeftButtonDown"
                                  PreviewMouseRightButtonDown="TreeViewItem_PreviewMouseRightButtonDown"
                                  PreviewKeyDown="FileTree_PreviewKeyDown">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="itemIcon"
                                                Grid.Column="0"
                                                Text="{Binding IconGlyph}"
                                                   FontFamily="Segoe MDL2 Assets"
                                                FontSize="14"
                                                   Foreground="{DynamicResource ThemeFileFg}"
                                                   Width="18"
                                                Margin="0,0,4,0"
                                                VerticalAlignment="Center"/>
                                        <TextBlock x:Name="itemText"
                                                Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   FontSize="12"
                                                Foreground="{DynamicResource ThemeFileFg}"
                                                   Margin="0,1"
                                                VerticalAlignment="Center"/>
                                        <TextBox x:Name="RenameBox"
                                                Grid.Column="1"
                                                 Visibility="Collapsed"
                                                 Text="{Binding Name, Mode=OneTime}"
                                                 FontSize="12"
                                                 Foreground="{DynamicResource ThemeFileFg}"
                                                 Background="{DynamicResource ThemeSettingsInputBg}"
                                                 BorderBrush="{DynamicResource ThemeSettingsAccent}"
                                                 BorderThickness="1"
                                                 CaretBrush="{DynamicResource ThemeFileFg}"
                                                 Padding="2,0"
                                                Margin="-3,0"
                                                 VerticalContentAlignment="Center"
                                                 KeyDown="RenameBox_KeyDown"
                                                 LostFocus="RenameBox_LostFocus"/>
                                        <Border x:Name="badgeBorder"
                                                Grid.Column="2"
                                                Background="{DynamicResource ThemeFolderExplorerHoverBg}"
                                                CornerRadius="2"
                                                Padding="4,0"
                                                Margin="4,0,8,0"
                                                VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility"
                                                            Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Badge}"
                                                                Value="{x:Null}">
                                                            <Setter Property="Visibility"
                                                                    Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock x:Name="badgeText"
                                                    Text="{Binding Badge}"
                                                    FontSize="9"
                                                    FontWeight="Bold"
                                                       Foreground="{DynamicResource ThemeFolderExplorerIconFg}"/>
                                        </Border>
                                    </Grid>
                                    <HierarchicalDataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsFolder}"
                                                Value="True">
                                            <Setter TargetName="itemText"
                                                    Property="FontWeight"
                                                    Value="SemiBold"/>
                                            <Setter TargetName="RenameBox"
                                                    Property="FontWeight"
                                                    Value="SemiBold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsRenaming}"
                                                Value="True">
                                            <Setter TargetName="itemText"
                                                    Property="Visibility"
                                                    Value="Collapsed"/>
                                            <Setter TargetName="RenameBox"
                                                    Property="Visibility"
                                                    Value="Visible"/>
                                        </DataTrigger>
                                        <!-- Color the type badge by kind: blue for disk images, green for
                                         confirmed BASIC, orange for machine language, purple for assembly
                                         source, pastel purple for plain-text BASIC source (.bas). Any
                                         other kind keeps the default neutral badge background set above. -->
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="D64">
                                            <Setter TargetName="badgeBorder"
                                                    Property="Background"
                                                    Value="{DynamicResource ThemeBadgeDiskBg}"/>
                                            <Setter TargetName="badgeText"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeBadgeDiskFg}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="D81">
                                            <Setter TargetName="badgeBorder"
                                                    Property="Background"
                                                    Value="{DynamicResource ThemeBadgeDiskBg}"/>
                                            <Setter TargetName="badgeText"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeBadgeDiskFg}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Ml">
                                            <Setter TargetName="badgeBorder"
                                                    Property="Background"
                                                    Value="{DynamicResource ThemeBadgeMlBg}"/>
                                            <Setter TargetName="badgeText"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeBadgeMlFg}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Prg">
                                            <Setter TargetName="badgeBorder"
                                                    Property="Background"
                                                    Value="{DynamicResource ThemeBadgePrgBg}"/>
                                            <Setter TargetName="badgeText"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeBadgePrgFg}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Asm">
                                            <Setter TargetName="badgeBorder"
                                                    Property="Background"
                                                    Value="{DynamicResource ThemeBadgeAsmBg}"/>
                                            <Setter TargetName="badgeText"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeBadgeAsmFg}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Bas">
                                            <Setter TargetName="badgeBorder"
                                                    Property="Background"
                                                    Value="{DynamicResource ThemeBadgeBasBg}"/>
                                            <Setter TargetName="badgeText"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeBadgeBasFg}"/>
                                        </DataTrigger>
                                        <!-- Highlights the file currently pending as the "left" side of a File
                                         Compare (see MainViewModel.PendingCompareFile). -->
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <MultiBinding Converter="{StaticResource PendingCompareHighlightConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Path="DataContext.ViewModel.PendingCompareFile"
                                                            RelativeSource="{RelativeSource AncestorType=Window}"/>
                                                </MultiBinding>
                                            </DataTrigger.Binding>
                                            <Setter TargetName="itemText"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeSettingsAccent}"/>
                                            <Setter TargetName="itemText"
                                                    Property="FontWeight"
                                                    Value="Bold"/>
                                            <Setter TargetName="itemIcon"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeSettingsAccent}"/>
                                        </DataTrigger>
                                    </HierarchicalDataTemplate.Triggers>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                            <TreeView.ItemContainerStyle>
                                <Style TargetType="TreeViewItem"
                                        BasedOn="{StaticResource ExplorerTreeItemStyle}">
                                    <EventSetter Event="PreviewMouseMove"
                                            Handler="FileTreeItem_PreviewMouseMove"/>
                                    <EventSetter Event="DragOver"
                                            Handler="FileTreeItem_DragOver"/>
                                    <EventSetter Event="DragLeave"
                                            Handler="FileTreeItem_DragLeave"/>
                                    <EventSetter Event="Drop"
                                            Handler="FileTreeItem_Drop"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFolder}"
                                                Value="True">
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource FolderContextMenu}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsFolder}"
                                                Value="False">
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource FileContextMenu}"/>
                                        </DataTrigger>
                                        <!-- .bas/.prg files confirmed to be BASIC get an "Open in BASIC editor" item;
                                         declared after the IsFolder=False trigger above so it wins for this Kind.
                                         Only Prg (a ready-to-transfer tokenized program) also gets Load/Run - Bas
                                         is untokenized ASCII source. -->
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Bas">
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource FileBasicContextMenu}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Prg">
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource FilePrgContextMenu}"/>
                                        </DataTrigger>
                                        <!-- Machine-language .prg files get "Disassemble file" instead. -->
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Ml">
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource FileMlContextMenu}"/>
                                        </DataTrigger>
                                        <!-- Assembly source files get "Open in Assembly editor" instead. -->
                                        <DataTrigger Binding="{Binding Kind}"
                                                Value="Asm">
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource FileAsmContextMenu}"/>
                                        </DataTrigger>
                                        <!-- A .d64/.d81 disk image node itself gets "Add File..." alongside the usual
                                         file actions; declared after Kind=Prg so it wins for disk images. -->
                                        <DataTrigger Binding="{Binding IsDiskImage}"
                                                Value="True">
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource LocalDiskImageContextMenu}"/>
                                        </DataTrigger>
                                        <!-- "Virtual" entries live only inside a disk image - Cut/Copy/Paste/Rename/
                                         Delete/Reveal above don't apply, so these triggers, declared last, win: no
                                         context menu by default, or just "Open in BASIC editor"/"Disassemble file"/
                                         "Open in Assembly editor"/hex-only for an unrecognized kind. -->
                                        <DataTrigger Binding="{Binding IsVirtual}"
                                                Value="True">
                                            <Setter Property="ContextMenu"
                                                    Value="{x:Null}"/>
                                        </DataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsVirtual}"
                                                        Value="True"/>
                                                <Condition Binding="{Binding IsOpenable}"
                                                        Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource LocalDiskEntryContextMenu}"/>
                                        </MultiDataTrigger>
                                        <!-- Declared after the general openable-virtual-entry trigger above so each
                                         wins for its own Kind specifically. -->
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsVirtual}"
                                                        Value="True"/>
                                                <Condition Binding="{Binding Kind}"
                                                        Value="Prg"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource LocalDiskEntryPrgContextMenu}"/>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsVirtual}"
                                                        Value="True"/>
                                                <Condition Binding="{Binding Kind}"
                                                        Value="Ml"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource LocalDiskEntryMlContextMenu}"/>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsVirtual}"
                                                        Value="True"/>
                                                <Condition Binding="{Binding Kind}"
                                                        Value="Asm"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource LocalDiskEntryAsmContextMenu}"/>
                                        </MultiDataTrigger>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsVirtual}"
                                                        Value="True"/>
                                                <Condition Binding="{Binding Kind}"
                                                        Value="Other"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="ContextMenu"
                                                    Value="{StaticResource LocalDiskEntryOtherContextMenu}"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TreeView.ItemContainerStyle>
                        </TreeView>
                    </DockPanel>
                </Grid>

                <GridSplitter x:Name="ExplorerRowSplitter"
                              Grid.Row="1"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              Height="4"
                              Background="{DynamicResource ThemeSplitter}"
                              ResizeDirection="Rows"
                              ResizeBehavior="PreviousAndNext"
                              ShowsPreview="False"
                              DragDelta="ExplorerRowSplitter_DragDelta"/>

                <!-- Variable Explorer: always visible regardless of which left panel (Explorer/C64U)
                 is active. Every variable in the active document, with each read/write
                 occurrence as a child node - click one to jump to that line. -->
                <DockPanel x:Name="VariablesPanel"
                        Grid.Row="2"
                        Background="{DynamicResource ThemeFolderExplorerBg}">
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                            BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                            BorderThickness="0,0,0,1"
                            Height="32">
                        <TextBlock Text="{Binding ViewModel.Variables.Count, StringFormat='VARIABLES ({0})'}"
                                   FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource ThemeFileFg}"
                                   VerticalAlignment="Center"
                                Margin="10,0,0,0"/>
                    </Border>

                    <TreeView x:Name="VariablesTree"
                              Background="{DynamicResource ThemeFolderExplorerBg}"
                              BorderThickness="0"
                              ItemsSource="{Binding ViewModel.Variables}"
                              MouseDoubleClick="VariablesTree_MouseDoubleClick"
                              PreviewKeyDown="VariablesTree_PreviewKeyDown"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling">
                        <TreeView.Resources>
                            <ContextMenu x:Key="VariableContextMenu">
                                <MenuItem Header="_Rename"
                                        InputGestureText="F2"
                                        Click="VariableContextRename_Click"/>
                            </ContextMenu>
                            <ContextMenu x:Key="VariableOccurrenceContextMenu">
                                <MenuItem Header="_Go to Line"
                                        Click="VariableOccurrenceContextGoToLine_Click"/>
                            </ContextMenu>

                            <HierarchicalDataTemplate DataType="{x:Type coremodels:VariableInfo}"
                                    ItemsSource="{Binding Occurrences}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="nameText"
                                            Grid.Column="0"
                                            Text="{Binding Name}"
                                               FontSize="12"
                                            Foreground="{DynamicResource ThemeFileFg}"
                                               Margin="0,1"
                                            VerticalAlignment="Center"/>
                                    <TextBox x:Name="RenameBox"
                                            Grid.Column="0"
                                             Visibility="Collapsed"
                                             Text="{Binding Name, Mode=OneTime}"
                                             CharacterCasing="Upper"
                                             FontSize="12"
                                             Foreground="{DynamicResource ThemeFileFg}"
                                             Background="{DynamicResource ThemeSettingsInputBg}"
                                             BorderBrush="{DynamicResource ThemeSettingsAccent}"
                                             BorderThickness="1"
                                             CaretBrush="{DynamicResource ThemeFileFg}"
                                             Padding="2,0"
                                            Margin="-3,0"
                                             VerticalContentAlignment="Center"
                                             KeyDown="VariableRenameBox_KeyDown"
                                             LostFocus="VariableRenameBox_LostFocus"/>
                                    <Border Grid.Column="1"
                                            Background="{DynamicResource ThemeFolderExplorerHoverBg}"
                                            CornerRadius="2"
                                            Padding="4,0"
                                            Margin="4,0,8,0"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{Binding TypeBadge}"
                                                FontSize="9"
                                                FontWeight="Bold"
                                                   Foreground="{DynamicResource ThemeFolderExplorerIconFg}"/>
                                    </Border>
                                </Grid>
                                <HierarchicalDataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsRenaming}"
                                            Value="True">
                                        <Setter TargetName="nameText"
                                                Property="Visibility"
                                                Value="Collapsed"/>
                                        <Setter TargetName="RenameBox"
                                                Property="Visibility"
                                                Value="Visible"/>
                                    </DataTrigger>
                                </HierarchicalDataTemplate.Triggers>
                            </HierarchicalDataTemplate>
                            <DataTemplate DataType="{x:Type coremodels:VariableOccurrenceInfo}">
                                <TextBlock Text="{Binding DisplayText}"
                                           FontSize="12"
                                        Foreground="{DynamicResource ThemeFileFg}"
                                           Margin="0,1"
                                        VerticalAlignment="Center"/>
                            </DataTemplate>

                            <Style x:Key="VariableTreeViewItemStyle"
                                    TargetType="TreeViewItem"
                                    BasedOn="{StaticResource ExplorerTreeItemStyle}">
                                <Setter Property="IsExpanded"
                                        Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                <Setter Property="ContextMenu"
                                        Value="{StaticResource VariableContextMenu}"/>
                                <EventSetter Event="PreviewMouseRightButtonDown"
                                        Handler="TreeViewItem_PreviewMouseRightButtonDown"/>
                            </Style>
                            <Style x:Key="VariableOccurrenceTreeViewItemStyle"
                                    TargetType="TreeViewItem"
                                    BasedOn="{StaticResource ExplorerTreeItemStyle}">
                                <Setter Property="ContextMenu"
                                        Value="{StaticResource VariableOccurrenceContextMenu}"/>
                                <EventSetter Event="PreviewMouseRightButtonDown"
                                        Handler="TreeViewItem_PreviewMouseRightButtonDown"/>
                            </Style>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyleSelector>
                            <local:VariableTreeItemStyleSelector VariableStyle="{StaticResource VariableTreeViewItemStyle}"
                                                                 OccurrenceStyle="{StaticResource VariableOccurrenceTreeViewItemStyle}"/>
                        </TreeView.ItemContainerStyleSelector>
                    </TreeView>
                </DockPanel>

                <!-- Symbol Explorer: assembly's equivalent of the Variable Explorer above - every
                 label/constant in the active document, with each definition/reference occurrence
                 as a child node. Occupies the same grid cell as VariablesPanel; MainWindow toggles
                 which of the two is visible based on the active tab's language. -->
                <DockPanel x:Name="SymbolsPanel"
                        Grid.Row="2"
                        Visibility="Collapsed"
                        Background="{DynamicResource ThemeFolderExplorerBg}">
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                            BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                            BorderThickness="0,0,0,1"
                            Height="32">
                        <TextBlock FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource ThemeFileFg}"
                                   VerticalAlignment="Center"
                                Margin="10,0,0,0">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="SYMBOLS ({0})"
                                        Converter="{StaticResource IntSumConverter}">
                                    <Binding Path="ViewModel.ConstantSymbols.Symbols.Count"/>
                                    <Binding Path="ViewModel.LabelSymbols.Symbols.Count"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </Border>

                    <TreeView x:Name="SymbolsTree"
                              Background="{DynamicResource ThemeFolderExplorerBg}"
                              BorderThickness="0"
                              ItemsSource="{Binding ViewModel.SymbolGroups}"
                              MouseDoubleClick="SymbolsTree_MouseDoubleClick"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling">
                        <TreeView.Resources>
                            <HierarchicalDataTemplate DataType="{x:Type coremodels:AsmSymbolGroupInfo}"
                                    ItemsSource="{Binding Symbols}">
                                <TextBlock FontSize="12"
                                        FontWeight="SemiBold"
                                           Foreground="{DynamicResource ThemeFileFg}"
                                           Margin="0,2"
                                        VerticalAlignment="Center">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0} ({1})">
                                            <Binding Path="Name"/>
                                            <Binding Path="Symbols.Count"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </HierarchicalDataTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type coremodels:AsmSymbolInfo}"
                                    ItemsSource="{Binding Occurrences}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                            Text="{Binding Name}"
                                               FontSize="12"
                                            Foreground="{DynamicResource ThemeFileFg}"
                                               Margin="0,1"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1"
                                            Text="{Binding ValueText}"
                                               FontSize="11"
                                            Foreground="{DynamicResource ThemeSettingsDescFg}"
                                               Margin="0,0,6,0"
                                            VerticalAlignment="Center"/>
                                    <Border Grid.Column="2"
                                            Background="{DynamicResource ThemeFolderExplorerHoverBg}"
                                            CornerRadius="2"
                                            Padding="4,0"
                                            Margin="4,0,8,0"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{Binding TypeBadge}"
                                                FontSize="9"
                                                FontWeight="Bold"
                                                   Foreground="{DynamicResource ThemeFolderExplorerIconFg}"/>
                                    </Border>
                                </Grid>
                            </HierarchicalDataTemplate>
                            <DataTemplate DataType="{x:Type coremodels:AsmSymbolOccurrenceInfo}">
                                <TextBlock Text="{Binding DisplayText}"
                                           FontSize="12"
                                        Foreground="{DynamicResource ThemeFileFg}"
                                           Margin="0,1"
                                        VerticalAlignment="Center"/>
                            </DataTemplate>
                            <Style x:Key="AsmSymbolExpandableTreeViewItemStyle"
                                    TargetType="TreeViewItem"
                                    BasedOn="{StaticResource ExplorerTreeItemStyle}">
                                <Setter Property="IsExpanded"
                                        Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            </Style>
                            <Style x:Key="AsmSymbolLeafTreeViewItemStyle"
                                    TargetType="TreeViewItem"
                                    BasedOn="{StaticResource ExplorerTreeItemStyle}"/>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyleSelector>
                            <local:AsmSymbolTreeItemStyleSelector ExpandableStyle="{StaticResource AsmSymbolExpandableTreeViewItemStyle}"
                                                                  LeafStyle="{StaticResource AsmSymbolLeafTreeViewItemStyle}"/>
                        </TreeView.ItemContainerStyleSelector>
                    </TreeView>
                </DockPanel>

                <!-- Left panel: C64U (FTP explorer) -->
                <Grid x:Name="C64UPanel"
                        Grid.Row="0"
                        Visibility="Collapsed">
                    <DockPanel Background="{DynamicResource ThemeFolderExplorerBg}">
                        <Border DockPanel.Dock="Top"
                                Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                                BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                                BorderThickness="0,0,0,1"
                                Height="32">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0"
                                        Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                        Margin="10,0,0,0">
                                    <Ellipse Width="8"
                                            Height="8"
                                            Margin="0,0,6,0"
                                            VerticalAlignment="Center">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill"
                                                        Value="#E74C3C"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ViewModel.IsC64UConnected}"
                                                            Value="True">
                                                        <Setter Property="Fill"
                                                                Value="#2ECC71"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock Text="{Binding ViewModel.C64UHeaderText}"
                                            FontSize="11"
                                            FontWeight="Bold"
                                               Foreground="{DynamicResource ThemeFileFg}"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                        Margin="0,0,4,0">
                                    <StackPanel Orientation="Horizontal">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ViewModel.IsC64UConnected}"
                                                            Value="True">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <Button Style="{StaticResource ExplorerHeaderButton}"
                                                ToolTip="New Folder"
                                                Click="C64UNewFolderToolbar_Click">
                                            <TextBlock Text="&#xED25;"
                                                    FontFamily="Segoe MDL2 Assets"
                                                    FontSize="13"
                                                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        </Button>
                                        <Button Style="{StaticResource ExplorerHeaderButton}"
                                                ToolTip="Upload File"
                                                Click="C64UUploadToolbar_Click">
                                            <TextBlock Text="&#xE898;"
                                                    FontFamily="Segoe MDL2 Assets"
                                                    FontSize="13"
                                                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        </Button>
                                        <Button Style="{StaticResource ExplorerHeaderButton}"
                                                ToolTip="Refresh"
                                                Click="C64URefreshToolbar_Click">
                                            <TextBlock Text="&#xE72C;"
                                                    FontFamily="Segoe MDL2 Assets"
                                                    FontSize="13"
                                                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                        </Button>
                                    </StackPanel>
                                    <Button Style="{StaticResource ExplorerHeaderButton}"
                                            ToolTip="C64U Settings"
                                            Click="C64USettingsHeader_Click">
                                        <TextBlock Text="&#xE713;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="13"
                                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border DockPanel.Dock="Bottom"
                                Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                                BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                                BorderThickness="0,1,0,0"
                                Padding="10,6"
                                Visibility="{Binding ViewModel.IsC64UConnected, Converter={StaticResource BoolToVisibility}}">
                            <StackPanel>
                                <Grid Margin="0,0,0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                            FontSize="11"
                                            Foreground="{DynamicResource ThemeFolderExplorerIconFg}"
                                               VerticalAlignment="Center">
                                    <Run Text="Drive A: "/><Run Text="{Binding ViewModel.C64UDriveALabel, Mode=OneWay}"
                                                Foreground="{DynamicResource ThemeFileFg}"/>
                                    </TextBlock>
                                    <Button Grid.Column="1"
                                            Style="{StaticResource TabCloseButtonStyle}"
                                            ToolTip="Eject Drive A"
                                            Click="C64UEjectDriveA_Click"
                                            Visibility="{Binding ViewModel.IsC64UDriveAMounted, Converter={StaticResource BoolToVisibility}}"/>
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                            FontSize="11"
                                            Foreground="{DynamicResource ThemeFolderExplorerIconFg}"
                                               VerticalAlignment="Center">
                                    <Run Text="Drive B: "/><Run Text="{Binding ViewModel.C64UDriveBLabel, Mode=OneWay}"
                                                Foreground="{DynamicResource ThemeFileFg}"/>
                                    </TextBlock>
                                    <Button Grid.Column="1"
                                            Style="{StaticResource TabCloseButtonStyle}"
                                            ToolTip="Eject Drive B"
                                            Click="C64UEjectDriveB_Click"
                                            Visibility="{Binding ViewModel.IsC64UDriveBMounted, Converter={StaticResource BoolToVisibility}}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Grid>
                            <!-- Not connected -->
                            <StackPanel VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    Margin="20"
                                        Visibility="{Binding ViewModel.IsC64UNotConnected, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="&#xE7F4;"
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="48"
                                           Foreground="{DynamicResource ThemeFolderExplorerIconFg}"
                                           HorizontalAlignment="Center"
                                        Margin="0,0,0,16"/>
                                <TextBlock Text="Not connected"
                                        FontSize="12"
                                           Foreground="{DynamicResource ThemeFileFg}"
                                           HorizontalAlignment="Center"
                                        Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding ViewModel.C64UFtpHost}"
                                        FontSize="11"
                                           Foreground="{DynamicResource ThemeFolderExplorerIconFg}"
                                           HorizontalAlignment="Center"
                                        Margin="0,0,0,12"/>
                                <Button Content="Connect"
                                        Padding="14,4"
                                        HorizontalAlignment="Center"
                                        Click="C64UConnect_Click"/>
                                <TextBlock Text="Enable FTP in Ultimate menu → Network Services → FTP file service"
                                           FontSize="10"
                                        Foreground="{DynamicResource ThemeFolderExplorerIconFg}"
                                           TextWrapping="Wrap"
                                        TextAlignment="Center"
                                        HorizontalAlignment="Center"
                                           MaxWidth="180"
                                        Margin="0,16,0,0"/>
                            </StackPanel>

                            <!-- Connecting -->
                            <TextBlock Text="Connecting…"
                                    FontSize="12"
                                    Foreground="{DynamicResource ThemeFileFg}"
                                       HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                       Visibility="{Binding ViewModel.IsC64UConnecting, Converter={StaticResource BoolToVisibility}}"/>

                            <!-- Connected -->
                            <TreeView x:Name="C64UFileTree"
                                      Background="{DynamicResource ThemeFolderExplorerBg}"
                                      BorderThickness="0"
                                      AllowDrop="True"
                                      ItemsSource="{Binding ViewModel.C64UFileItems}"
                                      Visibility="{Binding ViewModel.IsC64UConnected, Converter={StaticResource BoolToVisibility}}"
                                      MouseDoubleClick="C64UFileTree_MouseDoubleClick"
                                      PreviewMouseLeftButtonDown="C64UFileTree_PreviewMouseLeftButtonDown"
                                      PreviewMouseRightButtonDown="TreeViewItem_PreviewMouseRightButtonDown"
                                      PreviewKeyDown="C64UFileTree_PreviewKeyDown">
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:Name="itemIcon"
                                                    Grid.Column="0"
                                                    Text="{Binding IconGlyph}"
                                                       FontFamily="Segoe MDL2 Assets"
                                                    FontSize="14"
                                                       Foreground="{DynamicResource ThemeFileFg}"
                                                       Width="18"
                                                    Margin="0,0,4,0"
                                                    VerticalAlignment="Center"/>
                                            <TextBlock x:Name="itemText"
                                                    Grid.Column="1"
                                                       Text="{Binding Name}"
                                                       FontSize="12"
                                                    Foreground="{DynamicResource ThemeFileFg}"
                                                       Margin="0,1"
                                                    VerticalAlignment="Center"/>
                                            <TextBox x:Name="C64URenameBox"
                                                    Grid.Column="1"
                                                     Visibility="Collapsed"
                                                     Text="{Binding Name, Mode=OneTime}"
                                                     FontSize="12"
                                                     Foreground="{DynamicResource ThemeFileFg}"
                                                     Background="{DynamicResource ThemeSettingsInputBg}"
                                                     BorderBrush="{DynamicResource ThemeSettingsAccent}"
                                                     BorderThickness="1"
                                                     CaretBrush="{DynamicResource ThemeFileFg}"
                                                     Padding="2,0"
                                                    Margin="-3,0"
                                                     VerticalContentAlignment="Center"
                                                     KeyDown="C64URenameBox_KeyDown"
                                                     LostFocus="C64URenameBox_LostFocus"/>
                                            <Border x:Name="badgeBorder"
                                                    Grid.Column="2"
                                                    Background="{DynamicResource ThemeFolderExplorerHoverBg}"
                                                    CornerRadius="2"
                                                    Padding="4,0"
                                                    Margin="4,0,8,0"
                                                    VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Badge}"
                                                                    Value="{x:Null}">
                                                                <Setter Property="Visibility"
                                                                        Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock x:Name="badgeText"
                                                        Text="{Binding Badge}"
                                                        FontSize="9"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemeFolderExplorerIconFg}"/>
                                            </Border>
                                        </Grid>
                                        <HierarchicalDataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsFolder}"
                                                    Value="True">
                                                <Setter TargetName="itemText"
                                                        Property="FontWeight"
                                                        Value="SemiBold"/>
                                                <Setter TargetName="C64URenameBox"
                                                        Property="FontWeight"
                                                        Value="SemiBold"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsRenaming}"
                                                    Value="True">
                                                <Setter TargetName="itemText"
                                                        Property="Visibility"
                                                        Value="Collapsed"/>
                                                <Setter TargetName="C64URenameBox"
                                                        Property="Visibility"
                                                        Value="Visible"/>
                                            </DataTrigger>
                                            <!-- Color the type badge by kind: blue for disk images, green for
                                             confirmed BASIC, orange for machine language, purple for assembly
                                             source, pastel purple for plain-text BASIC source (.bas). Any
                                             other kind keeps the default neutral badge background set above. -->
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="D64">
                                                <Setter TargetName="badgeBorder"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeBadgeDiskBg}"/>
                                                <Setter TargetName="badgeText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeBadgeDiskFg}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="D81">
                                                <Setter TargetName="badgeBorder"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeBadgeDiskBg}"/>
                                                <Setter TargetName="badgeText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeBadgeDiskFg}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Ml">
                                                <Setter TargetName="badgeBorder"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeBadgeMlBg}"/>
                                                <Setter TargetName="badgeText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeBadgeMlFg}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Prg">
                                                <Setter TargetName="badgeBorder"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeBadgePrgBg}"/>
                                                <Setter TargetName="badgeText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeBadgePrgFg}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Asm">
                                                <Setter TargetName="badgeBorder"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeBadgeAsmBg}"/>
                                                <Setter TargetName="badgeText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeBadgeAsmFg}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Bas">
                                                <Setter TargetName="badgeBorder"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeBadgeBasBg}"/>
                                                <Setter TargetName="badgeText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeBadgeBasFg}"/>
                                            </DataTrigger>
                                            <!-- Highlights the file currently mounted on Drive A or B. -->
                                            <DataTrigger Value="True">
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource C64UMountedPathConverter}">
                                                        <Binding Path="FullPath"/>
                                                        <Binding Path="DataContext.ViewModel.C64UDriveA.ImageFile"
                                                                RelativeSource="{RelativeSource AncestorType=Window}"/>
                                                        <Binding Path="DataContext.ViewModel.C64UDriveB.ImageFile"
                                                                RelativeSource="{RelativeSource AncestorType=Window}"/>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <Setter TargetName="itemText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeSettingsAccent}"/>
                                                <Setter TargetName="itemText"
                                                        Property="FontWeight"
                                                        Value="Bold"/>
                                                <Setter TargetName="itemIcon"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeSettingsAccent}"/>
                                            </DataTrigger>
                                            <!-- Highlights the file currently pending as the "left" side of a
                                             File Compare (see MainViewModel.PendingCompareFile). -->
                                            <DataTrigger Value="True">
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource PendingCompareHighlightConverter}">
                                                        <Binding Path="."/>
                                                        <Binding Path="DataContext.ViewModel.PendingCompareFile"
                                                                RelativeSource="{RelativeSource AncestorType=Window}"/>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <Setter TargetName="itemText"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeSettingsAccent}"/>
                                                <Setter TargetName="itemText"
                                                        Property="FontWeight"
                                                        Value="Bold"/>
                                                <Setter TargetName="itemIcon"
                                                        Property="Foreground"
                                                        Value="{DynamicResource ThemeSettingsAccent}"/>
                                            </DataTrigger>
                                        </HierarchicalDataTemplate.Triggers>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="TreeViewItem"
                                            BasedOn="{StaticResource ExplorerTreeItemStyle}">
                                        <EventSetter Event="PreviewMouseMove"
                                                Handler="C64UFileTreeItem_PreviewMouseMove"/>
                                        <EventSetter Event="DragOver"
                                                Handler="C64UFileTreeItem_DragOver"/>
                                        <EventSetter Event="DragLeave"
                                                Handler="C64UFileTreeItem_DragLeave"/>
                                        <EventSetter Event="Drop"
                                                Handler="C64UFileTreeItem_Drop"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFolder}"
                                                    Value="True">
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UFolderContextMenu}"/>
                                            </DataTrigger>
                                            <!-- Bas and Prg are both "runnable" (IsRunnable), but only Prg (a
                                             ready-to-transfer tokenized program) gets Load/Run - Bas is
                                             untokenized ASCII source - so each gets its own Kind-specific
                                             trigger rather than sharing one IsRunnable-based menu. -->
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Bas">
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UBasicFileContextMenu}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Prg">
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UPrgFileContextMenu}"/>
                                            </DataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsFolder}"
                                                            Value="False"/>
                                                    <Condition Binding="{Binding IsRunnable}"
                                                            Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UOtherFileContextMenu}"/>
                                            </MultiDataTrigger>
                                            <!-- Machine-language files get "Disassemble file" instead of "Open in
                                             editor"; declared after the "other file" trigger above so it wins for
                                             this Kind (WPF applies the last-matching trigger in declaration order). -->
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Ml">
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UMlFileContextMenu}"/>
                                            </DataTrigger>
                                            <!-- Assembly source files get "Open in Assembly editor" instead. -->
                                            <DataTrigger Binding="{Binding Kind}"
                                                    Value="Asm">
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UAsmFileContextMenu}"/>
                                            </DataTrigger>
                                            <!-- Declared after the "other file"/Ml/Asm triggers above so it wins for
                                             disk images. -->
                                            <DataTrigger Binding="{Binding IsDiskImage}"
                                                    Value="True">
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UDiskImageContextMenu}"/>
                                            </DataTrigger>
                                            <!-- "Virtual" entries live only inside a disk image - none of the actions
                                             above (mount/run/download/rename/delete) apply, so these triggers,
                                             declared last, win: no context menu by default, or just "Open in
                                             BASIC editor"/"Disassemble file"/"Open in Assembly editor"/hex-only for
                                             an unrecognized kind. -->
                                            <DataTrigger Binding="{Binding IsVirtual}"
                                                    Value="True">
                                                <Setter Property="ContextMenu"
                                                        Value="{x:Null}"/>
                                            </DataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsVirtual}"
                                                            Value="True"/>
                                                    <Condition Binding="{Binding IsOpenable}"
                                                            Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UDiskEntryContextMenu}"/>
                                            </MultiDataTrigger>
                                            <!-- Declared after the general openable-virtual-entry trigger above so each
                                             wins for its own Kind specifically. -->
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsVirtual}"
                                                            Value="True"/>
                                                    <Condition Binding="{Binding Kind}"
                                                            Value="Prg"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UDiskEntryPrgContextMenu}"/>
                                            </MultiDataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsVirtual}"
                                                            Value="True"/>
                                                    <Condition Binding="{Binding Kind}"
                                                            Value="Ml"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UDiskEntryMlContextMenu}"/>
                                            </MultiDataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsVirtual}"
                                                            Value="True"/>
                                                    <Condition Binding="{Binding Kind}"
                                                            Value="Asm"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UDiskEntryAsmContextMenu}"/>
                                            </MultiDataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsVirtual}"
                                                            Value="True"/>
                                                    <Condition Binding="{Binding Kind}"
                                                            Value="Other"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="ContextMenu"
                                                        Value="{StaticResource C64UDiskEntryOtherContextMenu}"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                            </TreeView>
                        </Grid>
                    </DockPanel>
                </Grid>

                <!-- Left panel: Search (project-wide find/replace across the open project's files) -->
                <Grid x:Name="SearchPanel"
                        Grid.Row="0"
                        Visibility="Collapsed">
                    <Grid.Resources>
                        <Style x:Key="SearchToggleStyle"
                                TargetType="ToggleButton">
                            <Setter Property="Width"
                                    Value="24"/>
                            <Setter Property="Height"
                                    Value="22"/>
                            <Setter Property="FontSize"
                                    Value="11"/>
                            <Setter Property="Cursor"
                                    Value="Hand"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource ThemeFileFg}"/>
                            <Setter Property="BorderThickness"
                                    Value="1"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bd"
                                                CornerRadius="3"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver"
                                                    Value="True">
                                                <Setter TargetName="Bd"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeFolderExplorerHoverBg}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked"
                                                    Value="True">
                                                <Setter TargetName="Bd"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeFolderExplorerHoverBg}"/>
                                                <Setter TargetName="Bd"
                                                        Property="BorderBrush"
                                                        Value="{DynamicResource ThemeSettingsAccent}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                        <Style x:Key="SearchBtnStyle"
                                TargetType="Button">
                            <Setter Property="Height"
                                    Value="22"/>
                            <Setter Property="FontSize"
                                    Value="11"/>
                            <Setter Property="Cursor"
                                    Value="Hand"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource ThemeFileFg}"/>
                            <Setter Property="BorderThickness"
                                    Value="1"/>
                            <Setter Property="Padding"
                                    Value="6,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd"
                                                CornerRadius="3"
                                                Background="Transparent"
                                                BorderBrush="{DynamicResource ThemeSettingsInputBorder}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver"
                                                    Value="True">
                                                <Setter TargetName="Bd"
                                                        Property="Background"
                                                        Value="{DynamicResource ThemeFolderExplorerHoverBg}"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed"
                                                    Value="True">
                                                <Setter TargetName="Bd"
                                                        Property="Opacity"
                                                        Value="0.7"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Grid.Resources>
                    <DockPanel Background="{DynamicResource ThemeFolderExplorerBg}">
                        <StackPanel DockPanel.Dock="Top"
                                    Background="{DynamicResource ThemeFolderExplorerHeaderBg}">
                            <TextBlock Text="SEARCH"
                                       FontSize="11"
                                    FontWeight="Bold"
                                    Foreground="{DynamicResource ThemeFileFg}"
                                       Margin="10,8,0,6"/>
                            <DockPanel Margin="8,0,8,4"
                                    LastChildFill="True">
                                <ToggleButton x:Name="SearchReplaceExpandBtn"
                                              DockPanel.Dock="Left"
                                              Style="{StaticResource SearchToggleStyle}"
                                              Width="20"
                                              ToolTip="Toggle Replace"
                                              Checked="SearchReplaceExpandBtn_Checked"
                                              Unchecked="SearchReplaceExpandBtn_Unchecked">
                                    <TextBlock x:Name="SearchReplaceExpandArrow"
                                            Text="▸"
                                            FontSize="10"
                                               Foreground="{DynamicResource ThemeFileFg}"/>
                                </ToggleButton>
                                <StackPanel DockPanel.Dock="Right"
                                        Orientation="Horizontal"
                                        Margin="4,0,0,0">
                                    <ToggleButton x:Name="SearchMatchCaseBtn"
                                            Style="{StaticResource SearchToggleStyle}"
                                                  Content="Aa"
                                            ToolTip="Match Case"/>
                                    <ToggleButton x:Name="SearchWholeWordBtn"
                                            Style="{StaticResource SearchToggleStyle}"
                                                  Content="ab"
                                            ToolTip="Match Whole Word"/>
                                    <ToggleButton x:Name="SearchRegexBtn"
                                            Style="{StaticResource SearchToggleStyle}"
                                                  FontFamily="Consolas"
                                            Content=".*"
                                            ToolTip="Use Regular Expression"/>
                                </StackPanel>
                                <Grid Margin="4,0,0,0">
                                    <TextBox x:Name="SearchQueryBox"
                                             Height="22"
                                             BorderThickness="1"
                                             BorderBrush="{DynamicResource ThemeSettingsInputBorder}"
                                             Background="{DynamicResource ThemeSettingsInputBg}"
                                             Foreground="{DynamicResource ThemeFileFg}"
                                             CaretBrush="{DynamicResource ThemeFileFg}"
                                             FontFamily="Consolas"
                                            FontSize="12"
                                             Padding="4,2"
                                             VerticalContentAlignment="Center"
                                             KeyDown="SearchQueryBox_KeyDown"/>
                                    <TextBlock Text="Search"
                                            IsHitTestVisible="False"
                                               FontFamily="Consolas"
                                            FontSize="12"
                                               Foreground="{DynamicResource ThemeSettingsDescFg}"
                                               Margin="6,0,0,0"
                                            VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=SearchQueryBox}"
                                                            Value="">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DockPanel>
                            <DockPanel x:Name="SearchReplaceRow"
                                    Visibility="Collapsed"
                                       Margin="8,0,8,6"
                                    LastChildFill="True">
                                <!-- Spacer matching SearchReplaceExpandBtn's width, so the replace
                                 box lines up under the search box above. -->
                                <Rectangle DockPanel.Dock="Left"
                                        Width="20"
                                        Fill="Transparent"/>
                                <Button DockPanel.Dock="Right"
                                        Style="{StaticResource SearchBtnStyle}"
                                        Content="Replace All"
                                        Margin="4,0,0,0"
                                        ToolTip="Replace All in Project"
                                        Click="SearchReplaceAll_Click"/>
                                <Grid Margin="4,0,0,0">
                                    <TextBox x:Name="SearchReplaceBox"
                                             Height="22"
                                             BorderThickness="1"
                                             BorderBrush="{DynamicResource ThemeSettingsInputBorder}"
                                             Background="{DynamicResource ThemeSettingsInputBg}"
                                             Foreground="{DynamicResource ThemeFileFg}"
                                             CaretBrush="{DynamicResource ThemeFileFg}"
                                             FontFamily="Consolas"
                                            FontSize="12"
                                             Padding="4,2"
                                             VerticalContentAlignment="Center"/>
                                    <TextBlock Text="Replace"
                                            IsHitTestVisible="False"
                                               FontFamily="Consolas"
                                            FontSize="12"
                                               Foreground="{DynamicResource ThemeSettingsDescFg}"
                                               Margin="6,0,0,0"
                                            VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=SearchReplaceBox}"
                                                            Value="">
                                                        <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DockPanel>
                            <TextBlock x:Name="SearchStatusText"
                                       FontSize="11"
                                    Foreground="{DynamicResource ThemeSettingsDescFg}"
                                       Margin="10,0,0,8"
                                    Text=""/>
                        </StackPanel>

                        <TreeView x:Name="SearchResultsTree"
                                  Background="{DynamicResource ThemeFolderExplorerBg}"
                                  BorderThickness="0"
                                  ItemsSource="{Binding ViewModel.SearchResults}"
                                  MouseDoubleClick="SearchResultsTree_MouseDoubleClick">
                            <TreeView.Resources>
                                <HierarchicalDataTemplate DataType="{x:Type coremodels:ProjectSearchFileResult}"
                                        ItemsSource="{Binding Matches}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                Text="{Binding RelativeDisplayPath}"
                                                   FontSize="12"
                                                Foreground="{DynamicResource ThemeFileFg}"
                                                   Margin="0,1"
                                                VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <Border Grid.Column="1"
                                                Background="{DynamicResource ThemeFolderExplorerHoverBg}"
                                                CornerRadius="2"
                                                Padding="4,0"
                                                Margin="4,0,8,0"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Matches.Count}"
                                                    FontSize="9"
                                                    FontWeight="Bold"
                                                       Foreground="{DynamicResource ThemeFolderExplorerIconFg}"/>
                                        </Border>
                                    </Grid>
                                </HierarchicalDataTemplate>
                                <DataTemplate DataType="{x:Type coremodels:ProjectSearchMatchInfo}">
                                    <TextBlock Text="{Binding DisplayText}"
                                               FontSize="12"
                                            Foreground="{DynamicResource ThemeFileFg}"
                                               Margin="0,1"
                                            VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </TreeView.Resources>
                            <TreeView.ItemContainerStyle>
                                <Style TargetType="TreeViewItem"
                                        BasedOn="{StaticResource ExplorerTreeItemStyle}">
                                    <Setter Property="IsExpanded"
                                            Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                    <!-- Caught here, at the item itself (the event's source), rather
                                     than up on the TreeView - the TreeView's own template wraps
                                     its items in a ScrollViewer that would otherwise auto-scroll
                                     horizontally on selection before the event ever reached a
                                     handler up there. -->
                                    <EventSetter Event="RequestBringIntoView"
                                            Handler="SearchResultsTree_RequestBringIntoView"/>
                                </Style>
                            </TreeView.ItemContainerStyle>
                        </TreeView>
                    </DockPanel>
                </Grid>
            </Grid>

            <!-- Left GridSplitter -->
            <GridSplitter Grid.Column="2"
                          HorizontalAlignment="Stretch"
                          Background="{DynamicResource ThemeSplitter}"
                          ResizeBehavior="PreviousAndNext"
                          ShowsPreview="False"/>

            <!-- Main editor area: tab bar + editor -->
            <Grid Grid.Column="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Tab bar -->
                    <RowDefinition Height="*"/>
                    <!-- Editor -->
                    <RowDefinition x:Name="BottomPanelSplitterRow"
                            Height="0"/>
                    <!-- Bottom panel splitter -->
                    <RowDefinition x:Name="BottomPanelRow"
                            Height="0"
                            MinHeight="0"/>
                    <!-- Bottom panel (Debug/Errors) -->
                </Grid.RowDefinitions>

                <!-- Tab bar -->
                <Border Grid.Row="0"
                        Background="{DynamicResource ThemeTabBarBg}"
                        BorderBrush="{DynamicResource ThemeTabBorder}"
                        BorderThickness="0,0,0,1">
                    <DockPanel LastChildFill="True">
                        <!-- Shows a dropdown listing every open tab, so tabs scrolled out of view
                             at either end of the tab bar are still reachable in one click. Only
                             shown once the tab bar actually overflows - see TabBar_ScrollChanged. -->
                        <Button x:Name="TabListButton"
                                DockPanel.Dock="Right"
                                Style="{StaticResource ExplorerHeaderButton}"
                                ToolTip="Show Open Tabs"
                                Click="TabListButton_Click"
                                Visibility="Collapsed">
                            <TextBlock Text="&#xE712;"
                                    FontFamily="Segoe MDL2 Assets"
                                    FontSize="13"
                                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        </Button>
                        <ListBox x:Name="TabBar"
                                 ItemsSource="{Binding ViewModel.OpenTabs}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 SelectionChanged="TabBar_SelectionChanged"
                                 ItemContainerStyle="{StaticResource TabItemContainerStyle}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:EditorTab}">
                                    <Border Height="33"
                                            MinWidth="80"
                                            MaxWidth="220"
                                            ToolTip="{Binding FullPath}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:Name="TabFileName"
                                                       Grid.Column="0"
                                                       Text="{Binding FileName}"
                                                       VerticalAlignment="Center"
                                                       Padding="10,0,4,0"
                                                       FontSize="12"
                                                       Foreground="{DynamicResource ThemeTabInactiveFg}"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Grid Grid.Column="1"
                                                    Width="16"
                                                    Height="16"
                                                  VerticalAlignment="Center"
                                                    HorizontalAlignment="Center">
                                                <!-- Modified dot (shown when modified and tab not hovered) -->
                                                <Ellipse x:Name="ModifiedDot"
                                                         Width="8"
                                                        Height="8"
                                                         Fill="#6B69D6"
                                                         Visibility="Collapsed"/>
                                                <!-- Close button (shown when tab is hovered) -->
                                                <Button x:Name="CloseBtn"
                                                        Style="{StaticResource TabCloseButtonStyle}"
                                                        Click="TabClose_Click"
                                                        Visibility="Hidden"/>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <!-- Hovering: always show X -->
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                Value="True">
                                            <Setter TargetName="CloseBtn"
                                                    Property="Visibility"
                                                    Value="Visible"/>
                                            <Setter TargetName="ModifiedDot"
                                                    Property="Visibility"
                                                    Value="Collapsed"/>
                                        </DataTrigger>
                                        <!-- Not hovering + modified: show dot -->
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsModified}"
                                                        Value="True"/>
                                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                        Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <MultiDataTrigger.Setters>
                                                <Setter TargetName="ModifiedDot"
                                                        Property="Visibility"
                                                        Value="Visible"/>
                                            </MultiDataTrigger.Setters>
                                        </MultiDataTrigger>
                                        <!-- Active tab: brighter foreground -->
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                Value="True">
                                            <Setter TargetName="TabFileName"
                                                    Property="Foreground"
                                                    Value="{DynamicResource ThemeTabActiveFg}"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>
                </Border>

                <!-- Editor + Find Bar overlay -->
                <Grid Grid.Row="1"
                        Background="{DynamicResource ThemeEditorBg}">
                    <!-- Shown only while no tabs are open; ActivateTab toggles this and the Editor's
                         visibility together so an open tab always covers it. -->
                    <Image x:Name="EmptyStateImage"
                           Source="/ReadyCode;component/Assets/READYCode-01-tra-1024x1024.png"
                           Stretch="Uniform"
                           Width="320"
                            Height="320"
                           Opacity="0.5"
                           HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                           Visibility="Visible"/>
                    <!-- Groups the disassembly toolbar with Editor so the toolbar occupies a fixed
                         strip above the text rather than overlapping it - EditorContainer's own
                         Visibility (not Editor's) is what ActivateTab toggles for a text tab now,
                         so the toolbar hides/shows independently via DisassemblyToolbar.Visibility. -->
                    <Grid x:Name="EditorContainer"
                            Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <local:DisassemblyToolbarControl x:Name="DisassemblyToolbar"
                                Grid.Row="0"
                                                         DisassembleRequested="DisassemblyToolbar_DisassembleRequested"/>
                        <avalonEdit:TextEditor x:Name="Editor"
                                Grid.Row="1"
                                               Margin="0"
                                               Padding="10"
                                               FontFamily="../Assets/Fonts/#Pet Me 64"
                                               FontSize="16"
                                               Foreground="#000000"
                                               Background="{DynamicResource ThemeEditorBg}"
                                               WordWrap="False"
                                               ShowLineNumbers="False"
                                               HorizontalScrollBarVisibility="Auto"
                                               VerticalScrollBarVisibility="Auto"
                                               ContextMenu="{StaticResource EditorContextMenu}"
                                               TextChanged="Editor_TextChanged"
                                               PreviewKeyDown="Editor_PreviewKeyDown"
                                               PreviewTextInput="Editor_PreviewTextInput"
                                               MouseHover="Editor_MouseHover"
                                               MouseHoverStopped="Editor_MouseHoverStopped"
                                               PreviewMouseRightButtonDown="Editor_PreviewMouseRightButtonDown"/>
                    </Grid>
                    <local:HexEditorControl x:Name="HexEditor"
                            ContextMenu="{StaticResource HexEditorContextMenu}"/>
                    <local:FileCompareControl x:Name="CompareControl"/>
                    <local:FindBarControl x:Name="FindBar"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Top"
                                          Margin="0,0,20,0"
                                          Panel.ZIndex="10"
                                          Width="440"/>
                </Grid>

                <GridSplitter Grid.Row="2"
                        Height="4"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                              Background="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                              ResizeBehavior="PreviousAndNext"
                        ResizeDirection="Rows"/>

                <!-- Bottom panel: DEBUG (Variables / Breakpoints / Call Stack) and ERRORS tabs,
                     shown only while ViewModel.IsBottomPanelOpen (see ApplyBottomPanelOpenState
                     in code-behind, which sets BottomPanelRow/BottomPanelSplitterRow's heights to
                     match, mirroring how the left/right panel columns are toggled). Scoped to
                     this same Grid.Column="3" as the tab bar/editor above, rather than the whole
                     window, so it lines up with - and is exactly as wide as - the editor rather
                     than running under the side panels. -->
                <Border x:Name="BottomPanel"
                        Grid.Row="3"
                        Background="{DynamicResource ThemeFolderExplorerBg}"
                        BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                        BorderThickness="0,1,0,0">
                    <DockPanel>
                        <!-- Outer strip: DEBUG | ERRORS -->
                        <Border DockPanel.Dock="Top"
                                Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                                BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                                BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal">
                                    <ToggleButton x:Name="BottomPanelDebugTabToggle"
                                            Content="DEBUG"
                                            IsChecked="True"
                                                  Style="{StaticResource DebugPanelTabToggle}"
                                            Click="BottomPanelTab_Click"
                                            Tag="Debug"/>
                                    <ToggleButton x:Name="BottomPanelErrorsTabToggle"
                                            Content="ERRORS"
                                                  Style="{StaticResource DebugPanelTabToggle}"
                                            Click="BottomPanelTab_Click"
                                            Tag="Errors"/>
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Content="&#xE711;"
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="11"
                                        Padding="10,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{DynamicResource ThemeTabInactiveFg}"
                                        Click="CloseBottomPanel_Click"
                                        ToolTip="Close Panel"/>
                            </Grid>
                        </Border>

                        <Grid>
                            <!-- DEBUG tab content: today's exact Variables/Breakpoints/Call Stack
                                 strip + content grid, unchanged other than losing its own close
                                 button (the outer strip above now owns closing the whole panel). -->
                            <DockPanel x:Name="DebugPanelContent">
                                <Border DockPanel.Dock="Top"
                                        Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                                        BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                                        BorderThickness="0,0,0,1">
                                    <StackPanel Orientation="Horizontal">
                                        <ToggleButton x:Name="DebugVariablesTabToggle"
                                                Content="VARIABLES"
                                                IsChecked="True"
                                                      Style="{StaticResource DebugPanelTabToggle}"
                                                Click="DebugPanelTab_Click"
                                                Tag="Variables"/>
                                        <ToggleButton x:Name="DebugBreakpointsTabToggle"
                                                Content="BREAKPOINTS"
                                                      Style="{StaticResource DebugPanelTabToggle}"
                                                Click="DebugPanelTab_Click"
                                                Tag="Breakpoints"/>
                                        <ToggleButton x:Name="DebugCallStackTabToggle"
                                                Content="CALL STACK"
                                                      Style="{StaticResource DebugPanelTabToggle}"
                                                Click="DebugPanelTab_Click"
                                                Tag="CallStack"/>
                                    </StackPanel>
                                </Border>

                                <Grid>
                                    <!-- Value is the only editable column - editing commits via
                                         DebugVariablesGrid_CellEditEnding (in code-behind, using
                                         VariableWriteBack), not any two-way binding, since applying an
                                         edit means writing to the live machine and re-reading the true
                                         resulting value rather than trusting whatever was typed. It's a
                                         DataGridTemplateColumn (explicit CellTemplate/CellEditingTemplate)
                                         rather than a DataGridTextColumn deliberately - a
                                         DataGridTextColumn asks WPF to auto-generate its editing TextBox
                                         from the column's own Binding, which is fragile for a
                                         Path-less, Mode=OneWay, converter-only binding like this one and
                                         was found to never actually enter edit mode on double-click.
                                         DataGridTemplateColumn swaps templates directly with no
                                         binding-mode-dependent machinery involved. -->
                                    <DataGrid x:Name="DebugVariablesGrid"
                                            AutoGenerateColumns="False"
                                              Background="{DynamicResource ThemeFolderExplorerBg}"
                                            Foreground="{DynamicResource ThemeFileFg}"
                                              RowBackground="{DynamicResource ThemeFolderExplorerBg}"
                                              BorderThickness="0"
                                            GridLinesVisibility="All"
                                              HorizontalGridLinesBrush="{DynamicResource ThemeDataGridLineBrush}"
                                              VerticalGridLinesBrush="{DynamicResource ThemeDataGridLineBrush}"
                                              HeadersVisibility="Column"
                                              ColumnHeaderStyle="{StaticResource PanelDataGridColumnHeaderStyle}"
                                              CanUserAddRows="False"
                                            CanUserDeleteRows="False"
                                              ItemsSource="{Binding ViewModel.DebugVariables}"
                                              CellEditEnding="DebugVariablesGrid_CellEditEnding">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Name"
                                                    Binding="{Binding Name}"
                                                    Width="120"
                                                    IsReadOnly="True"/>
                                            <DataGridTextColumn Header="Type"
                                                    Binding="{Binding Type}"
                                                    Width="80"
                                                    IsReadOnly="True"/>
                                            <!-- FontFamily is "Pet Me 64" (same as the code editor) on both templates so any
                                                 PETSCII control-code bytes in a string value - invisible in a plain font,
                                                 and easy to silently destroy while editing a value that has them, since
                                                 VariableWriteBack.EncodeString space-pads to the original length - render
                                                 as their real C64 glyphs, via DebugVariableValueConverter's
                                                 PetsciiScreenCodeMap.ToDisplayText call. -->
                                            <DataGridTemplateColumn Header="Value"
                                                    Width="*">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Converter={StaticResource DebugVariableValueConverter}, Mode=OneWay}"
                                                                   FontFamily="../Assets/Fonts/#Pet Me 64"
                                                                   VerticalAlignment="Center"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                                <DataGridTemplateColumn.CellEditingTemplate>
                                                    <DataTemplate>
                                                        <TextBox Text="{Binding Converter={StaticResource DebugVariableValueConverter}, Mode=OneWay}"
                                                                 FontFamily="../Assets/Fonts/#Pet Me 64"
                                                                 BorderThickness="0"
                                                                VerticalContentAlignment="Center"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellEditingTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <DataGrid x:Name="DebugBreakpointsGrid"
                                            AutoGenerateColumns="False"
                                            Visibility="Collapsed"
                                              Background="{DynamicResource ThemeFolderExplorerBg}"
                                            Foreground="{DynamicResource ThemeFileFg}"
                                              RowBackground="{DynamicResource ThemeFolderExplorerBg}"
                                              BorderThickness="0"
                                            GridLinesVisibility="All"
                                              HorizontalGridLinesBrush="{DynamicResource ThemeDataGridLineBrush}"
                                              VerticalGridLinesBrush="{DynamicResource ThemeDataGridLineBrush}"
                                              HeadersVisibility="Column"
                                              ColumnHeaderStyle="{StaticResource PanelDataGridColumnHeaderStyle}"
                                              CanUserAddRows="False"
                                            CanUserDeleteRows="False"
                                              ItemsSource="{Binding ViewModel.BreakpointStore.Breakpoints}"
                                              MouseDoubleClick="DebugBreakpointsGrid_MouseDoubleClick">
                                        <DataGrid.Columns>
                                            <!-- A plain CheckBox in a template column, not
                                                 DataGridCheckBoxColumn - the latter only pushes its value
                                                 back to the source once the DataGrid's own cell-edit
                                                 lifecycle ends (the row losing focus), so the gutter dot
                                                 and live VICE session - both driven off Breakpoint's
                                                 PropertyChanged - didn't update until then either. A bare
                                                 CheckBox isn't tied to that lifecycle at all: with
                                                 UpdateSourceTrigger=PropertyChanged, clicking it commits
                                                 immediately. -->
                                            <DataGridTemplateColumn Header="On"
                                                    Width="40">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                  HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTextColumn Header="File"
                                                    Binding="{Binding FilePath}"
                                                    Width="*"
                                                    IsReadOnly="True"/>
                                            <DataGridTextColumn Header="Line"
                                                    Binding="{Binding LineNumber}"
                                                    Width="60"
                                                    IsReadOnly="True"/>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <ListView x:Name="DebugCallStackList"
                                            Visibility="Collapsed"
                                              Background="{DynamicResource ThemeFolderExplorerBg}"
                                            Foreground="{DynamicResource ThemeFileFg}"
                                              BorderThickness="0"
                                              ItemsSource="{Binding ViewModel.DebugCallStack}"
                                              MouseDoubleClick="DebugCallStackList_MouseDoubleClick">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding ReturnLineNumber, StringFormat=Returns to line {0}}"
                                                        Margin="4,2"/>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </DockPanel>

                            <!-- ERRORS tab content -->
                            <Grid x:Name="ErrorsPanelContent"
                                    Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0"
                                        Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                                        BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                                        BorderThickness="0,0,0,1"
                                        Padding="8,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                            <!-- "Open Files" (all open tabs) and "Active Tab" (just the current
                                                 one). A real folder-wide scan is future work - the dropdown
                                                 already being extensible is why adding this second option
                                                 wasn't a redesign. -->
                                            <ComboBox x:Name="ErrorsScopeCombo"
                                                    Width="110"
                                                    SelectedIndex="0"
                                                    Margin="0,0,16,0"
                                                      SelectionChanged="ErrorsScopeCombo_SelectionChanged">
                                                <ComboBoxItem Content="Open Files"/>
                                                <ComboBoxItem Content="Active Tab"/>
                                            </ComboBox>

                                            <ToggleButton x:Name="ErrorsSeverityErrorToggle"
                                                    IsChecked="True"
                                                          Style="{StaticResource ErrorsSeverityToggle}"
                                                    Margin="0,0,12,0"
                                                          Click="ErrorsSeverityToggle_Click"
                                                          ToolTip="Show/hide errors">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xEA39;"
                                                            FontFamily="Segoe MDL2 Assets"
                                                               Foreground="{DynamicResource ThemeStatusErrorBg}"
                                                            Margin="0,0,4,0"/>
                                                    <TextBlock Text="{Binding ViewModel.ErrorListRows.Count, StringFormat={}{0} Errors}"
                                                               Foreground="{DynamicResource ThemeSettingsLabelFg}"/>
                                                </StackPanel>
                                            </ToggleButton>

                                            <!-- Warnings/Messages: not interactive yet - nothing in
                                                 READYCode's diagnostics produces either severity today
                                                 (§10.2). Present for visual parity and so a future
                                                 softer check has somewhere to go. -->
                                            <ToggleButton IsEnabled="False"
                                                    Style="{StaticResource ErrorsSeverityToggle}"
                                                    Margin="0,0,12,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE7BA;"
                                                            FontFamily="Segoe MDL2 Assets"
                                                               Foreground="{DynamicResource ThemeStatusWarningBg}"
                                                            Margin="0,0,4,0"/>
                                                    <TextBlock Text="0 Warnings"
                                                            Foreground="{DynamicResource ThemeSettingsLabelFg}"/>
                                                </StackPanel>
                                            </ToggleButton>
                                            <ToggleButton IsEnabled="False"
                                                    Style="{StaticResource ErrorsSeverityToggle}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="&#xE946;"
                                                            FontFamily="Segoe MDL2 Assets"
                                                               Foreground="{DynamicResource ThemeSettingsLabelFg}"
                                                            Margin="0,0,4,0"/>
                                                    <TextBlock Text="0 Messages"
                                                            Foreground="{DynamicResource ThemeSettingsLabelFg}"/>
                                                </StackPanel>
                                            </ToggleButton>
                                        </StackPanel>

                                        <TextBox x:Name="ErrorsSearchBox"
                                                Grid.Column="2"
                                                Width="200"
                                                VerticalAlignment="Center"
                                                 Background="{DynamicResource ThemeSettingsInputBg}"
                                                Foreground="{DynamicResource ThemeFileFg}"
                                                 BorderBrush="{DynamicResource ThemeSettingsInputBorder}"
                                                BorderThickness="1"
                                                 Padding="6,3"
                                                TextChanged="ErrorsSearchBox_TextChanged"/>
                                    </Grid>
                                </Border>

                                <Grid Grid.Row="1">
                                    <DataGrid x:Name="ErrorsGrid"
                                            AutoGenerateColumns="False"
                                              Background="{DynamicResource ThemeFolderExplorerBg}"
                                            Foreground="{DynamicResource ThemeFileFg}"
                                              RowBackground="{DynamicResource ThemeFolderExplorerBg}"
                                              BorderThickness="0"
                                            GridLinesVisibility="All"
                                              HorizontalGridLinesBrush="{DynamicResource ThemeDataGridLineBrush}"
                                              VerticalGridLinesBrush="{DynamicResource ThemeDataGridLineBrush}"
                                              HeadersVisibility="Column"
                                              ColumnHeaderStyle="{StaticResource PanelDataGridColumnHeaderStyle}"
                                              CanUserAddRows="False"
                                            CanUserDeleteRows="False"
                                              ItemsSource="{Binding ViewModel.ErrorListRows}"
                                              MouseDoubleClick="ErrorsGrid_MouseDoubleClick">
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Header=""
                                                    Width="28"
                                                    IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="&#xEA39;"
                                                                FontFamily="Segoe MDL2 Assets"
                                                                   Foreground="{DynamicResource ThemeStatusErrorBg}"
                                                                   HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTextColumn Header="Description"
                                                    Binding="{Binding Message}"
                                                    Width="*"
                                                    IsReadOnly="True"/>
                                            <DataGridTextColumn Header="File"
                                                    Binding="{Binding FileName}"
                                                    Width="180"
                                                    IsReadOnly="True"/>
                                            <!-- Blank for an assembly row (BasicLineNumber is null there) - DataGridTextColumn
                                                 renders a null-valued binding as empty text with no converter needed. -->
                                            <DataGridTextColumn Header="BASIC Line"
                                                    Binding="{Binding BasicLineNumber}"
                                                    Width="90"
                                                    IsReadOnly="True"/>
                                            <DataGridTextColumn Header="Line"
                                                    Binding="{Binding Line}"
                                                    Width="70"
                                                    IsReadOnly="True"/>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <!-- Empty states, overlaid on top of the (then-empty) grid - see
                                         RefreshErrorsPanel/EnableLinting handling in code-behind. -->
                                    <TextBlock x:Name="ErrorsEmptyStateText"
                                            Text="No issues found."
                                               Foreground="{DynamicResource ThemeTabInactiveFg}"
                                               HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                               Visibility="Collapsed"
                                            IsHitTestVisible="False"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Right GridSplitter -->
            <GridSplitter Grid.Column="4"
                          HorizontalAlignment="Stretch"
                          Background="{DynamicResource ThemeSplitter}"
                          ResizeBehavior="PreviousAndNext"
                          ShowsPreview="False"/>

            <!-- Right panel: Special Characters, PETSCII Reference, BASIC Keywords -->
            <Grid Grid.Column="5">
                <!-- PETSCII Reference panel -->
                <DockPanel x:Name="PetsciiPanel"
                        Background="{DynamicResource ThemeFolderExplorerBg}"
                        Visibility="Collapsed">
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                            BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                            BorderThickness="0,0,0,1"
                            Height="32">
                        <TextBlock Text="PETSCII REFERENCE"
                                   FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource ThemeFileFg}"
                                   VerticalAlignment="Center"
                                Margin="10,0,0,0"/>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="PetsciiTablePanel"
                                Margin="8,8,0,0"/>
                    </ScrollViewer>
                </DockPanel>

                <!-- Quick Keys panel -->
                <DockPanel x:Name="SpecialCharsPanel"
                        Background="{DynamicResource ThemeFolderExplorerBg}">
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                            BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                            BorderThickness="0,0,0,1"
                            Height="32">
                        <TextBlock Text="QUICK KEYS"
                                   FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource ThemeFileFg}"
                                   VerticalAlignment="Center"
                                Margin="10,0,0,0"/>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <!-- Screen Control section -->
                            <TextBlock Text="SCREEN CONTROL"
                                       FontSize="10"
                                    FontWeight="Bold"
                                       Foreground="{DynamicResource ThemePanelHeaderFg}"
                                       Background="{DynamicResource ThemePanelHeaderBg}"
                                       MinHeight="22"
                                       VerticalAlignment="Center"
                                       Padding="8,4,0,4"
                                       Margin="8,8"/>
                            <TextBlock Text="CTRL+1–8"
                                       FontSize="9"
                                       Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                       Margin="8,0,8,4"/>
                            <WrapPanel Margin="6,0">
                                <!-- HOME — PETSCII 19 → screen code 0x93 → PUA U+E093 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="19"
                                        Click="SpecialChar_Click"
                                        ToolTip="Home: Ctrl+1; CHR$(19)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE093;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="HOME"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="1"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- CLR — PETSCII 147 → screen code 0xD3 → PUA U+E0D3 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="147"
                                        Click="SpecialChar_Click"
                                        ToolTip="CLR: Ctrl+2; CHR$(147)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D3;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="CLR"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="2"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- Reverse On — PETSCII 18 → screen code 0x92 → PUA U+E092 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="18"
                                        Click="SpecialChar_Click"
                                        ToolTip="Reverse On: Ctrl+3; CHR$(18)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE092;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="RVS ON"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="3"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- Reverse Off — PETSCII 146 → screen code 0xD2 → PUA U+E0D2 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="146"
                                        Click="SpecialChar_Click"
                                        ToolTip="Reverse Off: Ctrl+4; CHR$(146)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D2;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="RVS OFF"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="4"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- Cursor Left — PETSCII 157 → screen code 0xDD → PUA U+E0DD -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="157"
                                        Click="SpecialChar_Click"
                                        ToolTip="Cursor Left: Ctrl+5; CHR$(157)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0DD;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="CUR &#x2190;"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="5"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- Cursor Right — PETSCII 29 → screen code 0x9D → PUA U+E09D -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="29"
                                        Click="SpecialChar_Click"
                                        ToolTip="Cursor Right: Ctrl+6; CHR$(29)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE09D;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="CUR &#x2192;"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="6"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- Cursor Up — PETSCII 145 → screen code 0xD1 → PUA U+E0D1 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="145"
                                        Click="SpecialChar_Click"
                                        ToolTip="Cursor Up: Ctrl+7; CHR$(145)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D1;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="CUR &#x2191;"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="7"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- Cursor Down — PETSCII 17 → screen code 0x91 → PUA U+E091 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="17"
                                        Click="SpecialChar_Click"
                                        ToolTip="Cursor Down: Ctrl+8; CHR$(17)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE091;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="CUR &#x2193;"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="8"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                            </WrapPanel>

                            <!-- Printing Colors section -->
                            <TextBlock Text="PRINTING COLORS"
                                       FontSize="10"
                                    FontWeight="Bold"
                                       Foreground="{DynamicResource ThemePanelHeaderFg}"
                                       Background="{DynamicResource ThemePanelHeaderBg}"
                                       MinHeight="22"
                                       VerticalAlignment="Center"
                                       Padding="8,4,0,4"
                                       Margin="8,8"/>

                            <!-- CTRL+SHIFT+1–8 colors: Black White Red Cyan Purple Green Blue Yellow -->
                            <TextBlock Text="CTRL+SHIFT+1–8"
                                       FontSize="9"
                                       Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                       Margin="8,0,8,4"/>
                            <WrapPanel Margin="6,0">
                                <!-- Black — PETSCII 144 → screen code 0xD0 → PUA U+E0D0 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="144"
                                        Click="SpecialChar_Click"
                                        ToolTip="Black: Ctrl+Shift+1; CHR$(144); POKE(0)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D0;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#000000"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="BLACK"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="1"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="0"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- White — PETSCII 5 → screen code 0x85 → PUA U+E085 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="5"
                                        Click="SpecialChar_Click"
                                        ToolTip="White: Ctrl+Shift+2; CHR$(5); POKE(1)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE085;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#F7F7F7"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="WHITE"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="2"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="1"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Red — PETSCII 28 → screen code 0x9C → PUA U+E09C -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="28"
                                        Click="SpecialChar_Click"
                                        ToolTip="Red: Ctrl+Shift+3; CHR$(28); POKE(2)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE09C;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#8D2F34"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="RED"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="3"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="2"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Cyan — PETSCII 159 → screen code 0xDF → PUA U+E0DF -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="159"
                                        Click="SpecialChar_Click"
                                        ToolTip="Cyan: Ctrl+Shift+4; CHR$(159); POKE(3)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0DF;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#6AD4CD"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="CYAN"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="4"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="3"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Purple — PETSCII 156 → screen code 0xDC → PUA U+E0DC -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="156"
                                        Click="SpecialChar_Click"
                                        ToolTip="Purple: Ctrl+Shift+5; CHR$(156); POKE(4)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0DC;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#9835A4"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="PURPLE"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="5"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="4"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Green — PETSCII 30 → screen code 0x9E → PUA U+E09E -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="30"
                                        Click="SpecialChar_Click"
                                        ToolTip="Green: Ctrl+Shift+6; CHR$(30); POKE(5)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE09E;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#4CB442"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="GREEN"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="6"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="5"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Blue — PETSCII 31 → screen code 0x9F → PUA U+E09F -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="31"
                                        Click="SpecialChar_Click"
                                        ToolTip="Blue: Ctrl+Shift+7; CHR$(31); POKE(6)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE09F;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#2C29B1"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="BLUE"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="7"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="6"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Yellow — PETSCII 158 → screen code 0xDE → PUA U+E0DE -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="158"
                                        Click="SpecialChar_Click"
                                        ToolTip="Yellow: Ctrl+Shift+8; CHR$(158); POKE(7)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0DE;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#EFEF5D"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="YELLOW"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="8"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="7"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                            </WrapPanel>

                            <!-- CTRL+SHIFT+ALT+1–8 colors: Orange Brown Lt.Red Gray1 Gray2 Lt.Green Lt.Blue Gray3 -->
                            <TextBlock Text="CTRL+SHIFT+ALT+1–8"
                                       FontSize="9"
                                       Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                       Margin="8,8,8,4"/>
                            <WrapPanel Margin="6,0,6,8">
                                <!-- Orange — PETSCII 129 → screen code 0xC1 → PUA U+E0C1 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="129"
                                        Click="SpecialChar_Click"
                                        ToolTip="Orange: Ctrl+Shift+Alt+1; CHR$(129); POKE(8)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0C1;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#984E20"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="ORANGE"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="1"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="8"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Brown — PETSCII 149 → screen code 0xD5 → PUA U+E0D5 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="149"
                                        Click="SpecialChar_Click"
                                        ToolTip="Brown: Ctrl+Shift+Alt+2; CHR$(149); POKE(9)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D5;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#5B3800"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="BROWN"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="2"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="9"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Light Red — PETSCII 150 → screen code 0xD6 → PUA U+E0D6 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="150"
                                        Click="SpecialChar_Click"
                                        ToolTip="Light Red: Ctrl+Shift+Alt+3; CHR$(150); POKE(10)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D6;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#D1676D"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="LT. RED"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="3"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="10"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Gray 1 — PETSCII 151 → screen code 0xD7 → PUA U+E0D7 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="151"
                                        Click="SpecialChar_Click"
                                        ToolTip="Gray 1: Ctrl+Shift+Alt+4; CHR$(151); POKE(11)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D7;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#4A4A4A"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="GRAY 1"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="4"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="11"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Gray 2 — PETSCII 152 → screen code 0xD8 → PUA U+E0D8 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="152"
                                        Click="SpecialChar_Click"
                                        ToolTip="Gray 2: Ctrl+Shift+Alt+5; CHR$(152); POKE(12)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D8;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#7B7B7B"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="GRAY 2"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="5"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="12"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Light Green — PETSCII 153 → screen code 0xD9 → PUA U+E0D9 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="153"
                                        Click="SpecialChar_Click"
                                        ToolTip="Light Green: Ctrl+Shift+Alt+6; CHR$(153); POKE(13)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0D9;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#9FEF93"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="LT. GREEN"
                                                    FontFamily="Segoe UI"
                                                    FontSize="8"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="6"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="13"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Light Blue — PETSCII 154 → screen code 0xDA → PUA U+E0DA -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="154"
                                        Click="SpecialChar_Click"
                                        ToolTip="Light Blue: Ctrl+Shift+Alt+7; CHR$(154); POKE(14)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0DA;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#6D6AEF"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="LT. BLUE"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="7"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="14"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                                <!-- Gray 3 — PETSCII 155 → screen code 0xDB → PUA U+E0DB -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="155"
                                        Click="SpecialChar_Click"
                                        ToolTip="Gray 3: Ctrl+Shift+Alt+8; CHR$(155); POKE(15)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="&#xE0DB;"
                                                    FontFamily="../Assets/Fonts/#Pet Me 64"
                                                    FontSize="20"
                                                       Foreground="#B2B2B2"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="GRAY 3"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="8"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                        <TextBlock Text="15"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                   Margin="0,0,3,2"/>
                                    </Grid>
                                </Button>
                            </WrapPanel>

                            <!-- Function Keys section -->
                            <TextBlock Text="FUNCTION KEYS"
                                       FontSize="10"
                                    FontWeight="Bold"
                                       Foreground="{DynamicResource ThemePanelHeaderFg}"
                                       Background="{DynamicResource ThemePanelHeaderBg}"
                                       MinHeight="22"
                                       VerticalAlignment="Center"
                                       Padding="8,4,0,4"
                                       Margin="8,8"/>

                            <!-- SHIFT+F1-F8 (Shift+F3 does Find Previous instead while the Find bar is open) -->
                            <TextBlock Text="SHIFT+F1–F8"
                                       FontSize="9"
                                       Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                       Margin="8,0,8,4"/>
                            <WrapPanel Margin="6,0">
                                <!-- F1 — PETSCII 133 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="133"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 1: Shift+F1; CHR$(133)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F1"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 1"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F1"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- F2 — PETSCII 137 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="137"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 2: Shift+F2; CHR$(137)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F2"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 2"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F2"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- F3 — PETSCII 134. Shift+F3 inserts this only when the Find bar is closed;
                                     while it's open, Shift+F3 does Find Previous instead. -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="134"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 3: Shift+F3; CHR$(134)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F3"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 3"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F3"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- F4 — PETSCII 138 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="138"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 4: Shift+F4; CHR$(138)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F4"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 4"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F4"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- F5 — PETSCII 135 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="135"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 5: Shift+F5; CHR$(135)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F5"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 5"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F5"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- F6 — PETSCII 139 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="139"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 6: Shift+F6; CHR$(139)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F6"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 6"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F6"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- F7 — PETSCII 136 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="136"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 7: Shift+F7; CHR$(136)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F7"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 7"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F7"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                                <!-- F8 — PETSCII 140 -->
                                <Button Style="{StaticResource SpecialCharButtonStyle}"
                                        Tag="140"
                                        Click="SpecialChar_Click"
                                        ToolTip="Function 8: Shift+F8; CHR$(140)">
                                    <Grid Width="58"
                                            Height="54">
                                        <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center">
                                            <Border Background="{DynamicResource ThemePanelHeaderBg}"
                                                    CornerRadius="2"
                                                    Padding="6,2,6,2">
                                                <TextBlock Text="F8"
                                                        FontFamily="Segoe UI"
                                                        FontSize="14"
                                                        FontWeight="Bold"
                                                           Foreground="{DynamicResource ThemePanelHeaderFg}"
                                                        HorizontalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="FUNC 8"
                                                    FontFamily="Segoe UI"
                                                    FontSize="9"
                                                       Foreground="{DynamicResource ThemeSpecialCharLabelFg}"
                                                    HorizontalAlignment="Center"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Text="F8"
                                                FontFamily="Segoe UI"
                                                FontSize="8"
                                                FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeSpecialCharShortcutFg}"
                                                   HorizontalAlignment="Left"
                                                VerticalAlignment="Bottom"
                                                   Margin="3,0,0,2"/>
                                    </Grid>
                                </Button>
                            </WrapPanel>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>

                <!-- BASIC Keywords panel -->
                <DockPanel x:Name="BasicKeywordsPanel"
                        Background="{DynamicResource ThemeFolderExplorerBg}"
                        Visibility="Collapsed">
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                            BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                            BorderThickness="0,0,0,1"
                            Height="32">
                        <TextBlock Text="BASIC KEYWORDS"
                                   FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource ThemeFileFg}"
                                   VerticalAlignment="Center"
                                Margin="10,0,0,0"/>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="BasicKeywordsListPanel"/>
                    </ScrollViewer>
                </DockPanel>

                <!-- ASM Mnemonics panel -->
                <DockPanel x:Name="AsmKeywordsPanel"
                        Background="{DynamicResource ThemeFolderExplorerBg}"
                        Visibility="Collapsed">
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                            BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                            BorderThickness="0,0,0,1"
                            Height="32">
                        <TextBlock Text="ASM MNEMONICS"
                                   FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource ThemeFileFg}"
                                   VerticalAlignment="Center"
                                Margin="10,0,0,0"/>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="AsmKeywordsListPanel"/>
                    </ScrollViewer>
                </DockPanel>

                <!-- Music Notes panel -->
                <DockPanel x:Name="MusicNotesPanel"
                        Background="{DynamicResource ThemeFolderExplorerBg}"
                        Visibility="Collapsed">
                    <Border DockPanel.Dock="Top"
                            Background="{DynamicResource ThemeFolderExplorerHeaderBg}"
                            BorderBrush="{DynamicResource ThemeFolderExplorerHeaderBorder}"
                            BorderThickness="0,0,0,1"
                            Height="32">
                        <TextBlock Text="MUSIC NOTES"
                                   FontSize="11"
                                FontWeight="Bold"
                                Foreground="{DynamicResource ThemeFileFg}"
                                   VerticalAlignment="Center"
                                Margin="10,0,0,0"/>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                        <Grid x:Name="MusicNotesGrid"
                                Margin="8,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="32"/>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="48"/>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="50"/>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="30"/>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="36"/>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="50"/>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="30"/>
                                <ColumnDefinition Width="Auto"
                                        MinWidth="36"/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ScrollViewer>
                </DockPanel>
            </Grid>

            <!-- Right activity bar -->
            <Border Grid.Column="6"
                    Background="#3C3C3C"
                    BorderBrush="#2D2D2D"
                    BorderThickness="1,0,0,0">
                <StackPanel VerticalAlignment="Top">
                    <ToggleButton x:Name="SpecialCharsToggle"
                                  Style="{StaticResource ActivityBarButtonRight}"
                                  ToolTip="Quick Keys"
                                  Click="SpecialCharsToggle_Click">
                        <TextBlock Text="&#xE945;"
                                FontSize="18"
                                FontFamily="Segoe MDL2 Assets"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                    <ToggleButton x:Name="PetsciiToggle"
                                  Style="{StaticResource ActivityBarButtonRight}"
                                  ToolTip="PETSCII Reference"
                                  Click="PetsciiToggle_Click">
                        <TextBlock Text="&#x2663;"
                                FontSize="24"
                                FontWeight="Bold"
                                FontFamily="Segoe UI Symbol"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                    <ToggleButton x:Name="BasicKeywordsToggle"
                                  Style="{StaticResource ActivityBarButtonRight}"
                                  ToolTip="BASIC Keywords"
                                  Click="BasicKeywordsToggle_Click">
                        <TextBlock Text="&#xE82D;"
                                FontSize="18"
                                FontFamily="Segoe MDL2 Assets"
                                   Margin="2,4,0,0"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                    <ToggleButton x:Name="AsmKeywordsToggle"
                                  Style="{StaticResource ActivityBarButtonRight}"
                                  ToolTip="ASM Mnemonics"
                                  Click="AsmKeywordsToggle_Click">
                        <TextBlock Text="&#xE82D;"
                                FontSize="18"
                                FontFamily="Segoe MDL2 Assets"
                                   Margin="2,4,0,0"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                    <ToggleButton x:Name="MusicNotesToggle"
                                  Style="{StaticResource ActivityBarButtonRight}"
                                  ToolTip="Music Notes"
                                  Click="MusicNotesToggle_Click">
                        <TextBlock Text="&#x266B;"
                                FontSize="20"
                                FontFamily="Segoe UI Symbol"
                                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=ToggleButton}, Path=Foreground}"/>
                    </ToggleButton>
                </StackPanel>
            </Border>

        </Grid>
    </DockPanel>
</Window>
